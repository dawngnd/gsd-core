---
phase: 02-change-intake-and-spec-drafting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ndd-change-intake.cts
  - gsd-core/bin/lib/ndd-change-intake.cjs
  - tests/ndd-change-intake.test.cjs
autonomous: true
requirements: [NDD-03, INTK-02]
user_setup: []
must_haves:
  truths:
    - "NDD-03: NDD change state lives under `.planning/ndd/changes/<change-id>/` and never writes discovery artifacts into ROADMAP.md."
    - "INTK-02: A stable change id can be supplied explicitly or derived deterministically from the Markdown folder identity and file set."
    - "Change-id generation does not depend on timestamps, so the same folder/file set can be resumed predictably."
  artifacts:
    - "src/ndd-change-intake.cts exposes reusable helpers for change id validation, id derivation, workspace path resolution, and STATUS.json read/write."
    - "gsd-core/bin/lib/ndd-change-intake.cjs is generated from the TypeScript source by `npm run build:lib`."
    - "tests/ndd-change-intake.test.cjs covers id safety, deterministic generated ids, STATUS.json creation, and resume behavior."
  key_links:
    - "Later Phase 2 plans reuse the same intake module instead of duplicating path/id/status logic."
---

<objective>
Create the deterministic NDD change workspace foundation.

Purpose: `ndd-change` needs reliable file-system behavior before source ingestion and spec synthesis can be layered on top.
Output: reusable TypeScript helpers and tests for `.planning/ndd/changes/<change-id>/`, change-id generation, and `STATUS.json`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-change-intake-and-spec-drafting/02-CONTEXT.md
@src/planning-workspace.cts
@src/project-root.cts
@src/frontmatter.cts
@src/io.cts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NDD change workspace and id helpers</name>
  <files>src/ndd-change-intake.cts</files>
  <read_first>src/planning-workspace.cts, src/project-root.cts, src/io.cts, .planning/phases/02-change-intake-and-spec-drafting/02-CONTEXT.md</read_first>
  <action>Create `src/ndd-change-intake.cts` with pure helpers plus small filesystem boundary functions. Implement validation for explicit change ids using lowercase letters, numbers, and hyphen only. Reject empty ids, path separators, dot segments, leading dots, reserved names, and unsafe characters. Implement a resolver that returns `.planning/ndd/changes/<change-id>/` under a provided project root and guards against path escape.</action>
  <verify>npm run build:lib</verify>
  <acceptance_criteria>
    - Explicit valid ids resolve under `.planning/ndd/changes/`.
    - Unsafe ids return structured validation errors instead of being normalized into surprising values.
    - The resolver cannot produce a path outside the project root.
  </acceptance_criteria>
  <done>NDD has a safe workspace root and explicit id validation primitive.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic generated change ids</name>
  <files>src/ndd-change-intake.cts</files>
  <read_first>src/ndd-change-intake.cts</read_first>
  <action>Implement generated change ids using a slug from the docs folder basename plus a short deterministic suffix derived from the canonical docs folder path and discovered Markdown file relative paths. Do not include timestamps in the id. Keep the generated id stable for the same folder and Markdown file set, and let it change when the file set changes.</action>
  <verify>npm run build:lib</verify>
  <acceptance_criteria>
    - Re-running id generation against the same folder and Markdown file set returns the same id.
    - Adding or removing a Markdown file changes the deterministic suffix.
    - Folder names with spaces or uppercase characters produce safe lowercase hyphenated stems.
  </acceptance_criteria>
  <done>Users can omit an id and still get a stable resumable change id.</done>
</task>

<task type="auto">
  <name>Task 3: Write and resume STATUS.json</name>
  <files>src/ndd-change-intake.cts, tests/ndd-change-intake.test.cjs</files>
  <read_first>src/ndd-change-intake.cts, tests/verify.test.cjs</read_first>
  <action>Add functions that initialize a workspace directory and write `STATUS.json` with `change_id`, `status: \"draft\"`, `phase: \"intake\"`, `source_folder`, `source_count`, `artifacts`, `warnings`, `created_at`, and `updated_at`. If a workspace already exists, load existing `STATUS.json` when valid and preserve existing fields unless the caller intentionally updates intake metadata.</action>
  <verify>npm run build:lib && node --test tests/ndd-change-intake.test.cjs</verify>
  <acceptance_criteria>
    - `STATUS.json` is parseable JSON with the required fields.
    - Existing workspace resume does not clobber unrelated status fields.
    - Tests cover explicit id creation, unsafe id rejection, stable generated ids, file-set-sensitive ids, status creation, and resume.
  </acceptance_criteria>
  <done>The NDD change workspace can be created or resumed before ingestion starts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build:lib`
- [ ] `node --test tests/ndd-change-intake.test.cjs`
- [ ] `node .agents/gsd-core/bin/gsd-tools.cjs verify plan-structure .planning/phases/02-change-intake-and-spec-drafting/02-01-PLAN.md --raw`
</verification>

<success_criteria>
- NDD workspace paths always live under `.planning/ndd/changes/`.
- Change ids are safe, stable, and tested.
- `STATUS.json` exists and can be resumed by later Phase 2 plans.
- No ROADMAP mutation happens as part of change intake helpers.
</success_criteria>

<output>
After completion, create `.planning/phases/02-change-intake-and-spec-drafting/02-01-SUMMARY.md`.
</output>
