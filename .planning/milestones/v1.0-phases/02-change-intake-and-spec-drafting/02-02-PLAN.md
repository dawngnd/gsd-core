---
phase: 02-change-intake-and-spec-drafting
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/ndd-change-intake.cts
  - gsd-core/bin/lib/ndd-change-intake.cjs
  - tests/ndd-change-intake.test.cjs
autonomous: true
requirements: [INTK-01, INTK-03, INTK-05]
user_setup: []
must_haves:
  truths:
    - "INTK-01: A user-provided local folder containing Markdown documents can be ingested into an NDD change workspace."
    - "INTK-03: `SOURCE-MANIFEST.md` lists every ingested Markdown file and records an inferred role where possible."
    - "INTK-05: Ingested source files remain traceable from original relative path to copied workspace path."
  artifacts:
    - ".planning/ndd/changes/<change-id>/sources/ contains copied Markdown sources for an intake run."
    - ".planning/ndd/changes/<change-id>/SOURCE-MANIFEST.md is generated from intake metadata."
    - "tests/ndd-change-intake.test.cjs covers folder ingestion, manifest generation, duplicate basenames, invalid inputs, and role inference."
  key_links:
    - "The manifest uses the workspace/id/status helpers from Plan 02-01 and updates `STATUS.json` with source count and manifest path."
---

<objective>
Implement Markdown folder ingestion and source manifest generation.

Purpose: NDD needs a source-traceable intake record before it can synthesize a change spec.
Output: copied Markdown sources under the change workspace plus `SOURCE-MANIFEST.md`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-change-intake-and-spec-drafting/02-CONTEXT.md
@.planning/phases/02-change-intake-and-spec-drafting/02-01-PLAN.md
@src/ndd-change-intake.cts
@src/markdown-sectionizer.cts
@src/frontmatter.cts
@src/secrets.cts
@src/security.cts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discover and copy Markdown sources safely</name>
  <files>src/ndd-change-intake.cts, tests/ndd-change-intake.test.cjs</files>
  <read_first>src/ndd-change-intake.cts, src/security.cts, tests/ndd-change-intake.test.cjs</read_first>
  <action>Extend the intake module to accept a local docs folder path, recursively discover `.md` and `.markdown` files, and copy them into `sources/` under the resolved change workspace. Preserve relative path identity enough to avoid collisions when files share a basename. Use Node filesystem APIs and explicit path normalization; do not shell out for copying.</action>
  <verify>npm run build:lib && node --test tests/ndd-change-intake.test.cjs</verify>
  <acceptance_criteria>
    - Missing folder input fails with a clear error.
    - Folders with no Markdown files fail with a clear error.
    - `.md` and `.markdown` files are discovered recursively.
    - Duplicate basenames do not overwrite each other under `sources/`.
    - Copied destination paths stay inside the change workspace.
  </acceptance_criteria>
  <done>Markdown source folders can be materialized into NDD workspace sources safely.</done>
</task>

<task type="auto">
  <name>Task 2: Infer conservative source roles</name>
  <files>src/ndd-change-intake.cts, tests/ndd-change-intake.test.cjs</files>
  <read_first>src/markdown-sectionizer.cts, src/frontmatter.cts, src/ndd-change-intake.cts</read_first>
  <action>Add conservative role inference for ingested Markdown files: `api-doc`, `business-doc`, `proposal`, `acceptance`, and `unknown`. Use filename, title, headings, and frontmatter/keyword signals. Record a short evidence string for any inferred role. Prefer `unknown` when evidence is weak.</action>
  <verify>npm run build:lib && node --test tests/ndd-change-intake.test.cjs</verify>
  <acceptance_criteria>
    - API-like files can be classified as `api-doc` with evidence.
    - business/process-like files can be classified as `business-doc` with evidence.
    - proposal-like files can be classified as `proposal` with evidence.
    - acceptance-criteria-like files can be classified as `acceptance` with evidence.
    - uncertain files remain `unknown`.
  </acceptance_criteria>
  <done>Source manifest entries can explain role inference without pretending certainty.</done>
</task>

<task type="auto">
  <name>Task 3: Generate SOURCE-MANIFEST.md and update STATUS.json</name>
  <files>src/ndd-change-intake.cts, tests/ndd-change-intake.test.cjs</files>
  <read_first>src/ndd-change-intake.cts, .planning/phases/02-change-intake-and-spec-drafting/02-CONTEXT.md</read_first>
  <action>Generate `SOURCE-MANIFEST.md` in the change workspace. Include change id, source folder, a table with original relative path, workspace copied path, inferred role, evidence, and warnings, plus a warnings section. Update `STATUS.json` with `source_count`, manifest artifact path, warnings, and updated timestamp while preserving prior fields.</action>
  <verify>npm run build:lib && node --test tests/ndd-change-intake.test.cjs</verify>
  <acceptance_criteria>
    - `SOURCE-MANIFEST.md` lists every ingested Markdown file exactly once.
    - Each manifest row links original relative path to copied source path.
    - Manifest role/evidence fields are deterministic enough for assertions.
    - `STATUS.json` records `SOURCE-MANIFEST.md` in artifacts and the correct source count.
  </acceptance_criteria>
  <done>NDD intake produces a human-readable, source-traceable manifest.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build:lib`
- [ ] `node --test tests/ndd-change-intake.test.cjs`
- [ ] `node .agents/gsd-core/bin/gsd-tools.cjs verify plan-structure .planning/phases/02-change-intake-and-spec-drafting/02-02-PLAN.md --raw`
</verification>

<success_criteria>
- A Markdown folder can be ingested into `.planning/ndd/changes/<change-id>/sources/`.
- `SOURCE-MANIFEST.md` records source files, copied paths, roles, evidence, and warnings.
- Source traceability exists before draft spec synthesis.
- Invalid inputs fail clearly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-change-intake-and-spec-drafting/02-02-SUMMARY.md`.
</output>
