---
phase: 05-planning-bridge
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/ndd-plan-bridge.cts
  - gsd-core/bin/lib/ndd-plan-bridge.cjs
  - src/ndd-command-router.cts
  - gsd-core/bin/lib/ndd-command-router.cjs
  - commands/ndd/plan-phase.md
  - tests/ndd-plan-bridge.test.cjs
requirements:
  - PLAN-01
  - PLAN-02
  - PLAN-03
  - PLAN-04
autonomous: true
must_haves:
  truths:
    - "PLAN-01: Regression tests prove unapproved and critically ambiguous changes are rejected."
    - "PLAN-02: Regression tests prove approved NDD artifacts produce planner bridge context."
    - "PLAN-03: Regression tests or workflow assertions prove NDD keeps GSD planning conventions active."
    - "PLAN-04: Regression tests prove phase-link metadata is written in the NDD change folder."
  artifacts:
    - "tests/ndd-plan-bridge.test.cjs covers helper and router integration."
    - "Generated runtime outputs `gsd-core/bin/lib/ndd-plan-bridge.cjs` and `gsd-core/bin/lib/ndd-command-router.cjs` are included when source fixes are needed."
  key_links:
    - "Tests import compiled runtime helpers from `gsd-core/bin/lib` after build."
    - "Tests use Node built-in test runner and existing `tests/helpers.cjs` patterns."
---

<objective>
Add regression coverage for the Phase 5 planning bridge.

Purpose: The bridge is a critical gate between NDD discovery and implementation planning; tests must prove it rejects invalid changes and records traceability for approved ones.
Output: Focused Node test coverage for helper behavior, CLI routing, workflow invariants, and phase-link metadata.
</objective>

<execution_context>
@/home/dangnd/code/github/gsd-core/.codex/gsd-core/workflows/execute-plan.md
@/home/dangnd/code/github/gsd-core/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/04-discuss-and-approve-change-spec/04-VERIFICATION.md
@.planning/phases/05-planning-bridge/05-01-SUMMARY.md
@.planning/phases/05-planning-bridge/05-02-SUMMARY.md
@src/ndd-plan-bridge.cts
@src/ndd-command-router.cts
@commands/ndd/plan-phase.md
@tests/ndd-discuss-spec.test.cjs
</context>

## Artifacts this plan produces

- `tests/ndd-plan-bridge.test.cjs`
- Regression cases for:
  - unapproved status rejection,
  - critical unresolved ambiguity rejection,
  - approved artifact bridge context,
  - missing change-folder `CONTEXT.md` backfill from approved `CHANGE-SPEC.md` plus `IMPACT.md`,
  - CLI `ndd plan` behavior,
  - target GSD phase validation and phase-local bridge context write behavior,
  - `phase-link.md` write/update behavior,
  - workflow text preserving GSD plan-check/source-grounding references.

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Validate and bridge approved NDD change into GSD planning mechanics | Covered by helper and CLI tests |
| REQ | PLAN-01 approved gate | Covered by negative and positive tests |
| REQ | PLAN-02 bridge context | Covered by context content/path assertions |
| REQ | PLAN-02 missing change-folder CONTEXT.md backfill | Covered by backfill test from CHANGE-SPEC and IMPACT |
| REQ | PLAN-02 target GSD phase handoff | Covered by router/workflow assertions for phase-local bridge context |
| REQ | PLAN-03 GSD conventions active | Covered by workflow invariant assertions |
| REQ | PLAN-04 phase-link metadata | Covered by write/update assertions |
| CONTEXT | Phase 4 D-09 planning gate | Covered by unapproved rejection test |
| CONTEXT | Phase 4 D-14 read change-folder CONTEXT.md | Covered by bridge context assertion |
| RESEARCH | No Phase 5 RESEARCH.md present | Not applicable |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add planning bridge regression tests</name>
  <files>tests/ndd-plan-bridge.test.cjs</files>
  <read_first>
    - tests/ndd-discuss-spec.test.cjs
    - tests/helpers.cjs
    - src/ndd-plan-bridge.cts
    - src/ndd-command-router.cts
    - commands/ndd/plan-phase.md
  </read_first>
  <behavior>
    - PLAN-01: Draft or discussed status causes `preparePlanningBridge` and `gsd-tools ndd plan` to fail.
    - PLAN-01: Approved status with critical unresolved ambiguity still fails.
    - PLAN-02: Approved change with `CHANGE-SPEC.md`, `IMPACT.md`, and `CONTEXT.md` returns bridge context containing all three artifact refs.
    - PLAN-02: Approved change missing change-folder `CONTEXT.md` causes the helper to create that file from `CHANGE-SPEC.md` resolved decisions, acceptance criteria, scope, and `IMPACT.md` before returning bridge context.
    - PLAN-02: Router or helper behavior proves a supplied target phase is validated and phase-local bridge context is written for canonical planner consumption.
    - PLAN-03: `commands/ndd/plan-phase.md` contains references to canonical GSD plan-phase, plan-check/source-grounding behavior, and does not define an independent planner flow.
    - PLAN-04: Link recording writes `phase-link.md` in `.planning/ndd/changes/<change-id>/` and records phase id plus plan files.
  </behavior>
  <action>
    Create `tests/ndd-plan-bridge.test.cjs` using Node built-in `node:test`, `node:assert/strict`, and the existing `tests/helpers.cjs` temp directory and `runGsdTools` patterns. Build realistic change workspaces under a temp project root with `STATUS.json`, `CHANGE-SPEC.md`, `IMPACT.md`, and `CONTEXT.md`; also build a fixture where `CONTEXT.md` is absent but approved `CHANGE-SPEC.md` and `IMPACT.md` exist. Import the compiled helper from `../gsd-core/bin/lib/ndd-plan-bridge.cjs` after the build step, matching existing NDD tests. Cover direct helper behavior and CLI behavior for `gsd-tools ndd plan`. Assert missing `CONTEXT.md` is backfilled inside `.planning/ndd/changes/<change-id>/` from approved spec and impact inputs before bridge context is returned. Assert target phase validation and phase-local bridge context writing through `gsd-tools ndd plan-context <change-id> <phase-id>` or the equivalent router command. Assert that `phase-link.md` is written under the change workspace and includes the change id, phase id, and plan file names. Add a workflow invariant assertion that reads `commands/ndd/plan-phase.md` and checks it references canonical GSD plan-phase plus source-grounding/plan-check/link steps so PLAN-03 remains guarded.
  </action>
  <acceptance_criteria>
    The new test file fails before the Phase 5 helper/router/workflow exist, passes after 05-01 and 05-02 are implemented, and covers all PLAN-01 through PLAN-04 requirements with focused assertions, including missing NDD `CONTEXT.md` backfill and target-phase handoff.
  </acceptance_criteria>
  <done>`tests/ndd-plan-bridge.test.cjs` covers approval gating, critical ambiguity gating, missing NDD `CONTEXT.md` backfill, bridge context creation, target-phase handoff, CLI behavior, workflow invariants, and phase-link metadata.</done>
  <verify>
    <automated>npx tsc --project tsconfig.build.json</automated>
    <automated>node --test tests/ndd-plan-bridge.test.cjs</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Run focused Phase 5 quality gates</name>
  <files>src/ndd-plan-bridge.cts, src/ndd-command-router.cts, commands/ndd/plan-phase.md, tests/ndd-plan-bridge.test.cjs</files>
  <read_first>
    - package.json
    - eslint.config.mjs
    - tests/ndd-plan-bridge.test.cjs
  </read_first>
  <action>
    Run the focused build, lint, and test commands for Phase 5 touched artifacts. If the new regression tests expose small defects in `src/ndd-plan-bridge.cts`, `src/ndd-command-router.cts`, or `commands/ndd/plan-phase.md`, fix only those Phase 5 files and regenerate the corresponding `gsd-core/bin/lib/*.cjs` outputs with `npx tsc --project tsconfig.build.json`. If `src/ndd-command-router.cts` changes existing NDD subcommand behavior, also run `node --test tests/ndd-discuss-spec.test.cjs` to guard the Phase 4 gate contract. Keep fixes scoped to the files declared in this plan and do not modify unrelated source or planning files.
  </action>
  <acceptance_criteria>
    TypeScript build passes, generated CommonJS outputs are current for touched TypeScript files, ESLint passes for Phase 5 touched source/test files, the new planning bridge tests pass, and existing NDD discussion tests still pass if router behavior changed.
  </acceptance_criteria>
  <done>Focused Phase 5 build, generated runtime outputs, lint, new bridge tests, and existing NDD discussion tests pass with changes scoped to Phase 5 artifacts.</done>
  <verify>
    <automated>npx tsc --project tsconfig.build.json</automated>
    <automated>npx eslint src/ndd-plan-bridge.cts src/ndd-command-router.cts tests/ndd-plan-bridge.test.cjs</automated>
    <automated>node --test tests/ndd-plan-bridge.test.cjs</automated>
    <automated>node --test tests/ndd-discuss-spec.test.cjs</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture to helper | Synthetic user Markdown and status files exercise bridge validation. |
| CLI wrapper to filesystem | Tests verify untrusted CLI args cannot write phase-link outside the NDD workspace. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-09 | Tampering | `phase-link.md` tests | medium | mitigate | Assert link output path remains under `.planning/ndd/changes/<change-id>/`. |
| T-05-10 | Spoofing | Approved state tests | medium | mitigate | Test rejected states and approved state with critical ambiguity independently. |
| T-05-11 | Repudiation | Link metadata tests | low | mitigate | Assert link metadata includes change id, phase id, plan files, and timestamp field. |
| T-05-12 | Denial of Service | Test scope | low | accept | Focused tests use tiny fixtures and Node built-in runner; no external services. |
</threat_model>

<verification>
Overall checks: TypeScript build, focused ESLint, new bridge tests, and existing discussion gate tests.
</verification>

<success_criteria>
Plan 05-03 is complete when automated coverage proves approved-spec gating, bridge context creation, source-grounded workflow conventions, and NDD phase-link metadata.
</success_criteria>

<output>
Create `.planning/phases/05-planning-bridge/05-03-SUMMARY.md` when done.
</output>
