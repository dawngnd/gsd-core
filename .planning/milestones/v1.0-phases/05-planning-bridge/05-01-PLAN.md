---
phase: 05-planning-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ndd-plan-bridge.cts
  - gsd-core/bin/lib/ndd-plan-bridge.cjs
requirements:
  - PLAN-01
  - PLAN-02
  - PLAN-04
autonomous: true
must_haves:
  truths:
    - "PLAN-01: Approved NDD changes can be distinguished from draft or discussed changes before GSD planning starts."
    - "PLAN-02: Approved CHANGE-SPEC.md, IMPACT.md, and NDD CONTEXT.md become explicit planner-readable bridge context."
    - "PLAN-02: Approved changes without change-folder CONTEXT.md are backfilled from CHANGE-SPEC.md resolved decisions, acceptance criteria, scope, and IMPACT.md instead of failing before planning."
    - "PLAN-04: The NDD change workspace has deterministic metadata for produced GSD phase and plan linkage."
  artifacts:
    - "src/ndd-plan-bridge.cts exports gate, context rendering, and phase-link helpers."
    - "gsd-core/bin/lib/ndd-plan-bridge.cjs is emitted by the TypeScript build and committed with the runtime source change."
  key_links:
    - "Reuse resolveChangeWorkspace and STATUS.json conventions from src/ndd-change-intake.cts."
    - "Reuse checkDiscussionApproval semantics from src/ndd-discuss-spec.cts without copying discussion logic."
---

<objective>
Define the deterministic bridge model from an approved NDD change artifact set to the GSD planner context and phase-link metadata.

Purpose: Phase 5 needs a small TypeScript helper boundary for validation, bridge context rendering, and link recording while keeping GSD planner internals untouched.
Output: A focused `src/ndd-plan-bridge.cts` helper planned with concrete exports and behavior for downstream workflow/router work.
</objective>

<execution_context>
@/home/dangnd/code/github/gsd-core/.codex/gsd-core/workflows/execute-plan.md
@/home/dangnd/code/github/gsd-core/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-change-intake-and-spec-drafting/02-CONTEXT.md
@.planning/phases/03-impact-discovery/03-CONTEXT.md
@.planning/phases/04-discuss-and-approve-change-spec/04-CONTEXT.md
@.planning/phases/04-discuss-and-approve-change-spec/04-VERIFICATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@src/ndd-change-intake.cts
@src/ndd-discuss-spec.cts
</context>

## Artifacts this plan produces

- `src/ndd-plan-bridge.cts`
- `gsd-core/bin/lib/ndd-plan-bridge.cjs` generated by `npx tsc --project tsconfig.build.json`
- Export contract for Phase 5 wrapper and tests:
  - `preparePlanningBridge({ projectRoot, changeId, phaseId? })`
  - `ensureChangeContext({ projectRoot, changeId })`
  - `renderPlannerBridgeContext(...)`
  - `writePhaseLink({ projectRoot, changeId, phaseId, phaseDir, planFiles })`
  - structured result types with `ok: true` or `ok: false`

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | `ndd-plan-phase` validates approved change and bridges into GSD planning mechanics | Covered by bridge gate and context renderer |
| REQ | PLAN-01 approved `CHANGE-SPEC.md` gate | Covered by `preparePlanningBridge` |
| REQ | PLAN-02 planner-readable bridge context | Covered by `renderPlannerBridgeContext` |
| REQ | PLAN-02 approved change missing NDD CONTEXT.md | Covered by `ensureChangeContext` backfill from CHANGE-SPEC and IMPACT |
| REQ | PLAN-04 phase/plan linkage metadata | Covered by `writePhaseLink` |
| CONTEXT | Phase 4 D-08 dual approval write | Covered by checking both STATUS.json and CHANGE-SPEC frontmatter when available |
| CONTEXT | Phase 4 D-09 planning refusal gate | Covered by structured gate failures |
| CONTEXT | Phase 4 D-14 read NDD CONTEXT.md from change folder | Covered by bridge context inputs |
| RESEARCH | No Phase 5 RESEARCH.md present | Not applicable |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic NDD planning bridge helper</name>
  <files>src/ndd-plan-bridge.cts</files>
  <read_first>
    - src/ndd-change-intake.cts
    - src/ndd-discuss-spec.cts
    - .planning/phases/04-discuss-and-approve-change-spec/04-CONTEXT.md
    - .planning/phases/04-discuss-and-approve-change-spec/04-VERIFICATION.md
  </read_first>
  <behavior>
    - PLAN-01: `preparePlanningBridge` returns `ok: false` when `STATUS.json.status` is not `approved`.
    - PLAN-01: `preparePlanningBridge` returns `ok: false` when discussion approval check reports critical unresolved ambiguity.
    - PLAN-02: Approved changes return normalized paths and contents or path refs for `CHANGE-SPEC.md`, `IMPACT.md`, and change-folder `CONTEXT.md`.
    - PLAN-02: If `.planning/ndd/changes/<change-id>/CONTEXT.md` is absent, `ensureChangeContext` creates it from `CHANGE-SPEC.md` resolved decisions, acceptance criteria, scope, approval status, and `IMPACT.md` impact summary before bridge rendering continues.
    - PLAN-04: `writePhaseLink` writes or updates `phase-link.md` under `.planning/ndd/changes/<change-id>/` without leaving NDD discovery metadata outside the change folder.
  </behavior>
  <action>
    Create `src/ndd-plan-bridge.cts` as a small deterministic helper module. Reuse `resolveChangeWorkspace`, `validateChangeId`, and STATUS conventions from `src/ndd-change-intake.cts`. Reuse `checkDiscussionApproval` or its public semantics from `src/ndd-discuss-spec.cts` for the critical ambiguity gate. Do not import or duplicate GSD planner internals. Implement file-system reads and writes with Node APIs plus existing cross-platform write helpers where the surrounding NDD modules already use them.

    Define the concrete bridge model in this helper. `preparePlanningBridge` must gate on approved status and critical ambiguity first, then call `ensureChangeContext`. `ensureChangeContext` must treat existing `.planning/ndd/changes/<change-id>/CONTEXT.md` as authoritative when present; when it is absent, it must create a planner-readable NDD context file in that exact path by extracting or summarizing from `CHANGE-SPEC.md` Resolved Decisions, Acceptance Criteria, Scope, approval metadata, and `IMPACT.md` impacted files/risks/open questions. If `CHANGE-SPEC.md` or `IMPACT.md` is missing, return a structured missing-artifact error; do not fail merely because the NDD context file was not created by Phase 4. Mark backfilled context with a generated-source note and source artifact refs so the planner can audit provenance.

    The helper should verify required artifacts after context backfill: `STATUS.json`, `CHANGE-SPEC.md`, `IMPACT.md`, and `CONTEXT.md`. Render bridge context as markdown that cites the NDD change id, source artifact paths, approval state, target GSD phase candidate or selected phase, impact summary path, context path, and source-grounding instructions for the planner. The bridge context must instruct the planner to keep normal GSD plan-check/source-grounding conventions active per PLAN-03, even though PLAN-03 workflow wiring lands in 05-02. `phase-link.md` should include change id, GSD phase id or phase directory, plan file list, linked_at timestamp, and source artifact refs. Ensure `npx tsc --project tsconfig.build.json` emits `gsd-core/bin/lib/ndd-plan-bridge.cjs`; commit the generated runtime output with the source file.
  </action>
  <acceptance_criteria>
    `src/ndd-plan-bridge.cts` exists, has no dependency on GSD plan-phase implementation internals, exposes structured helper functions, refuses unapproved or critically ambiguous changes, creates missing NDD `CONTEXT.md` from approved `CHANGE-SPEC.md` and `IMPACT.md`, renders planner-readable context from NDD artifacts, and writes `phase-link.md` only inside the NDD change workspace. `gsd-core/bin/lib/ndd-plan-bridge.cjs` is generated by the build.
  </acceptance_criteria>
  <done>`src/ndd-plan-bridge.cts` exists, exposes the planned helper contract, enforces approval and critical-ambiguity gates, backfills missing NDD `CONTEXT.md`, renders NDD bridge context, writes scoped phase-link metadata, and emits `gsd-core/bin/lib/ndd-plan-bridge.cjs` via build.</done>
  <verify>
    <automated>npx tsc --project tsconfig.build.json</automated>
    <automated>npx eslint src/ndd-plan-bridge.cts</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/user change id to filesystem | Untrusted change id resolves to `.planning/ndd/changes/<change-id>/`. |
| NDD artifacts to planner prompt | User-provided Markdown content is transformed into planning context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | `resolveChangeWorkspace` usage | high | mitigate | Reuse existing change-id validation and path containment; never concatenate unvalidated change ids into paths. |
| T-05-02 | Spoofing | Approval gate | medium | mitigate | Check machine-readable `STATUS.json.status` and discuss gate output before producing bridge context. |
| T-05-03 | Information Disclosure | Planner bridge context | medium | mitigate | Reference source artifact paths and scoped excerpts only; do not scan unrelated project files in this helper. |
| T-05-04 | Repudiation | `phase-link.md` | low | mitigate | Write timestamped phase/plan linkage metadata under the NDD change folder. |
</threat_model>

<verification>
Overall checks: `npx tsc --project tsconfig.build.json` and `npx eslint src/ndd-plan-bridge.cts`.
</verification>

<success_criteria>
Plan 05-01 is complete when a deterministic NDD planning bridge helper exists and can be consumed by the wrapper workflow and regression tests without forking GSD planner internals.
</success_criteria>

<output>
Create `.planning/phases/05-planning-bridge/05-01-SUMMARY.md` when done.
</output>
