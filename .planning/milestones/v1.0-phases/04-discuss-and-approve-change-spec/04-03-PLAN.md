---
wave: 3
depends_on: [04-01-PLAN.md, 04-02-PLAN.md]
files_modified:
  - tests/ndd-discuss-spec.test.cjs
autonomous: true
requirements: [DISC-01, DISC-02, DISC-03, DISC-04, DISC-05]
---

# Plan 04-03: Tests for Critical Ambiguity Blocking and Approved Spec Gating

## Tasks

<task id="04-03-T1">
<title>Unit tests for extractAmbiguities, severity classification, and writeResolvedSection</title>

<read_first>
- tests/ndd-impact-discovery.test.cjs (test structure pattern: describe/test blocks, beforeEach/afterEach with createTempDir/cleanup, assert patterns, import pattern for compiled module)
- tests/helpers.cjs (createTempDir, cleanup, runGsdTools signatures)
- gsd-core/bin/lib/ndd-discuss-spec.cjs (compiled module — the functions to test: extractAmbiguities, classifyAmbiguitySeverity, buildDiscussionSeeds, writeResolvedSection, updateApprovalStatus, hasCriticalUnresolved)
- src/ndd-discuss-spec.cts (source types and function signatures for reference)
- .planning/phases/04-discuss-and-approve-change-spec/04-CONTEXT.md (D-10 through D-12 severity rules, D-04/D-05 resolved section rules, D-22 unit test expectations)
</read_first>

<action>
Create `tests/ndd-discuss-spec.test.cjs` with the following test structure using `node:test` and `node:assert/strict`:

**Import block**: `require('../gsd-core/bin/lib/ndd-discuss-spec.cjs')` for the discuss module, plus `fs`, `path`, and `{ createTempDir, cleanup, runGsdTools }` from `./helpers.cjs`.

**describe('NDD discuss spec')** with beforeEach creating tmpDir, changeWorkspaceDir with STATUS.json (phase: impact, status: draft), CHANGE-SPEC.md (with Ambiguities, Conflicts, Open Questions sections containing test bullets), and IMPACT.md.

1. **describe('extractAmbiguities')**:
   - test: extracts ambiguities from `## Ambiguities` section as array
   - test: extracts conflicts from `## Conflicts` section
   - test: extracts open questions from `## Open Questions` section
   - test: returns empty arrays when sections are missing
   - test: returns empty arrays when sections have no bullets
   - test: each ambiguity has `severity`, `text`, `section_ref` fields

2. **describe('classifyAmbiguitySeverity')**:
   - test: returns `'critical'` for text containing `'api contract'`
   - test: returns `'critical'` for text containing `'database schema'`
   - test: returns `'critical'` for text containing `'authentication'`
   - test: returns `'critical'` for text containing `'breaking change'`
   - test: returns `'non-critical'` for text like `'UI color preference unclear'`
   - test: returns `'non-critical'` for text like `'Documentation wording TBD'`

3. **describe('buildDiscussionSeeds')**:
   - test: merges CHANGE-SPEC ambiguities with IMPACT.md unknowns
   - test: handles null impactContent gracefully
   - test: critical ambiguities appear before non-critical in sorted output

4. **describe('writeResolvedSection')**:
   - test: appends `## Resolved` section to CHANGE-SPEC.md
   - test: preserves original `## Ambiguities` section content after write
   - test: preserves original `## Conflicts` section content after write
   - test: appends `## Acceptance Criteria` section
   - test: appends `## Scope` section with `### In Scope` and `### Out of Scope`
   - test: calling twice replaces previous `## Resolved` section (no duplication)
   - test: each resolution bullet contains original_ref, decision, and source fields

5. **describe('hasCriticalUnresolved')**:
   - test: returns true when seeds contain critical ambiguity
   - test: returns false when seeds contain only non-critical ambiguities
   - test: returns false when seeds have empty ambiguities array
</action>

<acceptance_criteria>
- File `tests/ndd-discuss-spec.test.cjs` exists
- All tests pass: `node --test tests/ndd-discuss-spec.test.cjs` exits with code 0
- At least 6 test cases for `classifyAmbiguitySeverity` covering both critical and non-critical returns
- At least 6 test cases for `writeResolvedSection` covering preservation, append, and idempotency
- At least 3 test cases for `hasCriticalUnresolved` covering the DISC-05 blocking gate
- Test file imports from `../gsd-core/bin/lib/ndd-discuss-spec.cjs` (compiled output)
- Test file uses `createTempDir`/`cleanup` from `./helpers.cjs`
</acceptance_criteria>

<verify>
Run: `node --test tests/ndd-discuss-spec.test.cjs` — all tests pass
Run: `grep -c 'test(' tests/ndd-discuss-spec.test.cjs` — at least 20 test cases
</verify>
</task>

<task id="04-03-T2">
<title>Integration tests for approval gate, dual-write, and CLI discuss subcommands</title>

<read_first>
- tests/ndd-discuss-spec.test.cjs (the file created in T1 — append to it)
- tests/ndd-impact-discovery.test.cjs (CLI integration test pattern at line 422-447: runGsdTools usage, success/error assertions, JSON parsing)
- tests/helpers.cjs (runGsdTools signature and return shape)
- .planning/phases/04-discuss-and-approve-change-spec/04-CONTEXT.md (D-07 through D-09 for approval flow, D-22/D-23 for test strategy)
</read_first>

<action>
Append to `tests/ndd-discuss-spec.test.cjs`:

1. **describe('updateApprovalStatus')**:
   - test: sets STATUS.json status to `'approved'` and phase to `'approved'`
   - test: writes `approval_status: approved` into CHANGE-SPEC.md frontmatter (add frontmatter if needed, or update existing)
   - test: updates `- Status:` and `- Approval:` lines in CHANGE-SPEC.md body
   - test: returns ok:false for non-existent workspace
   - test: preserves existing STATUS.json fields (change_id, source_folder, artifacts) after update

2. **describe('DISC-05 gate: critical ambiguity blocks planning')** (integration):
   - test: `discuss-check` returns `has_critical_unresolved: true` when CHANGE-SPEC.md contains critical ambiguity in `## Ambiguities` section
   - test: `discuss-check` returns `has_critical_unresolved: false` when only non-critical ambiguities remain
   - test: `discuss-check` returns `approved: false` when STATUS.json status is not `'approved'`
   - test: after calling `discuss-update <id> approved`, `discuss-check` returns `approved: true`

3. **describe('CLI integration: ndd discuss subcommands')**:
   - test: `gsd-tools ndd discuss <change-id>` returns JSON with `ambiguities` array
   - test: `gsd-tools ndd discuss` without args returns error containing `'Usage'`
   - test: `gsd-tools ndd discuss-update <change-id> approved` succeeds and returns ok:true
   - test: `gsd-tools ndd discuss-update <change-id> invalid` returns error about invalid status
   - test: `gsd-tools ndd discuss-check <change-id>` returns JSON with `has_critical_unresolved` and `approved` fields
   - test: `gsd-tools ndd discuss-check` without args returns error containing `'Usage'`

For all tests, use the same beforeEach/afterEach pattern from T1 with pre-populated workspace containing STATUS.json, CHANGE-SPEC.md with test ambiguities (at least one critical), and IMPACT.md.
</action>

<acceptance_criteria>
- All tests pass: `node --test tests/ndd-discuss-spec.test.cjs` exits with code 0
- At least 5 test cases for `updateApprovalStatus` covering dual-write consistency
- At least 4 test cases for DISC-05 gate covering both blocking and non-blocking scenarios
- At least 6 CLI integration tests covering all three subcommands (discuss, discuss-update, discuss-check)
- Tests verify dual-write: both STATUS.json and CHANGE-SPEC.md are checked after approval
- Tests verify DISC-05: critical ambiguity presence is detected and reported correctly
</acceptance_criteria>

<verify>
Run: `node --test tests/ndd-discuss-spec.test.cjs` — all tests pass
Run: `grep -c 'test(' tests/ndd-discuss-spec.test.cjs` — at least 35 test cases total
</verify>
</task>

## must_haves

These must be TRUE after this plan executes — derived goal-backward from all four phase success criteria:

1. Tests prove `extractAmbiguities` correctly parses CHANGE-SPEC.md sections into structured ambiguity/conflict/question data (SC-1)
2. Tests prove `classifyAmbiguitySeverity` correctly distinguishes critical from non-critical ambiguities (SC-3)
3. Tests prove `writeResolvedSection` preserves original CHANGE-SPEC.md content while appending Resolved, Acceptance Criteria, and Scope sections (SC-2)
4. Tests prove `updateApprovalStatus` dual-writes to both STATUS.json and CHANGE-SPEC.md frontmatter (SC-4)
5. Tests prove `hasCriticalUnresolved` returns true when critical ambiguities exist, blocking the planning gate (SC-3)
6. CLI integration tests prove all three discuss subcommands (discuss, discuss-update, discuss-check) work end-to-end (SC-1 through SC-4)

## Artifacts this plan produces

- `tests/ndd-discuss-spec.test.cjs` — comprehensive test file covering:
  - extractAmbiguities unit tests
  - classifyAmbiguitySeverity unit tests
  - buildDiscussionSeeds unit tests
  - writeResolvedSection unit tests
  - hasCriticalUnresolved unit tests
  - updateApprovalStatus integration tests
  - DISC-05 gate integration tests
  - CLI integration tests for discuss, discuss-update, discuss-check subcommands
