---
phase: 3
plan: 03-03
type: test
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - tests/ndd-impact-discovery.test.cjs
autonomous: true
requirements: [IMPT-01, IMPT-02, IMPT-03, IMPT-04]
---

<objective>
Add unit and integration tests for the impact discovery module covering:
- Capability config loading (valid, invalid, missing)
- IMPACT.md rendering (structure, tables, frontmatter, empty groups)
- Codebase map availability detection (present, absent, empty)
- CHANGE-SPEC requirement section extraction
- `runImpactDiscovery` orchestrator (workspace validation, status transitions, IMPACT.md creation)
- Missing codebase map behavior (structured warning in result)
- CLI integration via `runGsdTools('ndd impact <change-id>')`
</objective>

<tasks>

### Task 1: Create Test File Skeleton and Fixtures

<action>
Create `tests/ndd-impact-discovery.test.cjs` following the test pattern from `tests/ndd-change-intake.test.cjs`:

```javascript
const { describe, test, beforeEach, afterEach } = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');
const { createTempDir, cleanup } = require('./helpers.cjs');

const impact = require('../gsd-core/bin/lib/ndd-impact-discovery.cjs');
```

In `beforeEach`:
1. Create `tmpDir = createTempDir('gsd-ndd-impact-')`
2. Create a pre-populated change workspace at `path.join(tmpDir, '.planning', 'ndd', 'changes', 'test-change')` with:
   - `STATUS.json` containing: `{ change_id: 'test-change', status: 'draft', phase: 'intake', source_folder: '/tmp/sources', source_count: 2, artifacts: { source_manifest: 'SOURCE-MANIFEST.md', change_spec: 'CHANGE-SPEC.md' }, warnings: [], created_at: '2026-07-08T00:00:00.000Z', updated_at: '2026-07-08T00:00:00.000Z' }`
   - `CHANGE-SPEC.md` with content including `## Confirmed Source-Backed Requirements` section and two bullet items: `- The checkout flow must support saved cards.` and `- Payment API must validate card tokens.`
3. Create `.planning/codebase/` directory with a sample `ARCHITECTURE.md` file (minimal content like `# Architecture\n\nSample map content.\n`)

In `afterEach`: call `cleanup(tmpDir)`.
</action>

<read_first>
- tests/ndd-change-intake.test.cjs (lines 1–30 for test skeleton pattern)
- tests/helpers.cjs (createTempDir, cleanup, runGsdTools signatures)
- src/ndd-impact-discovery.cts (exports to test)
- src/ndd-change-intake.cts (NddChangeStatus interface at lines 83–94 for fixture shape)
</read_first>

<acceptance_criteria>
- `tests/ndd-impact-discovery.test.cjs` exists
- Imports `node:test`, `node:assert/strict`, `helpers.cjs`, and `ndd-impact-discovery.cjs`
- `beforeEach` creates a temp directory with a valid change workspace fixture
- `afterEach` calls `cleanup(tmpDir)`
- File runs without import errors: `node --test tests/ndd-impact-discovery.test.cjs` (may have 0 tests initially)
</acceptance_criteria>

<verify>
```bash
node --test tests/ndd-impact-discovery.test.cjs 2>&1 | head -5
```
</verify>

---

### Task 2: Capability Config Loading Tests

<action>
Add a `describe('loadCodebaseCapabilityConfig', ...)` block with these tests:

1. **`returns default config when file not found`**:
   - Call `impact.loadCodebaseCapabilityConfig('/nonexistent-path')`
   - Assert result has `tools` key
   - Assert `result.tools.grep_search.available === true`
   - Assert `result.tools.codegraph.available === false`
   - Assert `result.tools.wiki.available === false`

2. **`parses valid JSON correctly`**:
   - Create `path.join(tmpDir, 'gsd-core', 'ndd', 'codebase_capability.json')` with a custom config where `codegraph.available` is `true`
   - Call `impact.loadCodebaseCapabilityConfig(tmpDir)`
   - Assert `result.tools.codegraph.available === true`

3. **`returns default for invalid JSON`**:
   - Create the config file with content `'{not valid json}'`
   - Call `impact.loadCodebaseCapabilityConfig(tmpDir)`
   - Assert result has `tools` key (defaults returned)
   - Assert `result.tools.grep_search.available === true`

4. **`returns default for missing tools key`**:
   - Create the config file with content `'{}'` (valid JSON but no `tools`)
   - Call `impact.loadCodebaseCapabilityConfig(tmpDir)`
   - Assert result has `tools` key with defaults
</action>

<read_first>
- tests/ndd-impact-discovery.test.cjs (current state after Task 1)
- src/ndd-impact-discovery.cts (loadCodebaseCapabilityConfig implementation)
- gsd-core/ndd/codebase_capability.json (default config content)
</read_first>

<acceptance_criteria>
- 4 tests exist under `describe('loadCodebaseCapabilityConfig')`
- All 4 tests pass: `node --test tests/ndd-impact-discovery.test.cjs`
- Tests cover: missing file, valid file, invalid JSON, missing `tools` key
</acceptance_criteria>

<verify>
```bash
node --test --test-name-pattern='loadCodebaseCapabilityConfig' tests/ndd-impact-discovery.test.cjs
```
</verify>

---

### Task 3: Codebase Map Availability Tests

<action>
Add a `describe('checkCodebaseMapAvailability', ...)` block with:

1. **`returns available true with file list when codebase maps exist`**:
   - The fixture already created `.planning/codebase/ARCHITECTURE.md` in beforeEach
   - Call `impact.checkCodebaseMapAvailability(tmpDir)`
   - Assert `result.available === true`
   - Assert `result.files` is an array containing `'ARCHITECTURE.md'`
   - Assert `result.files.length >= 1`

2. **`returns available false when directory is missing`**:
   - Create a fresh temp dir without `.planning/codebase/`
   - Call `impact.checkCodebaseMapAvailability(freshDir)`
   - Assert `result.available === false`
   - Assert `result.files` is an empty array `[]`

3. **`returns available false when directory is empty`**:
   - Create `.planning/codebase/` dir but put no `.md` files (or only non-md files)
   - Call `impact.checkCodebaseMapAvailability(dirWithEmpty)`
   - Assert `result.available === false`
   - Assert `result.files` is an empty array
</action>

<read_first>
- tests/ndd-impact-discovery.test.cjs (current state)
- src/ndd-impact-discovery.cts (checkCodebaseMapAvailability implementation)
</read_first>

<acceptance_criteria>
- 3 tests exist under `describe('checkCodebaseMapAvailability')`
- All pass: present directory returns `available: true`, missing returns `false`, empty returns `false`
- Test verifies `files` array contents, not just `available` boolean
</acceptance_criteria>

<verify>
```bash
node --test --test-name-pattern='checkCodebaseMapAvailability' tests/ndd-impact-discovery.test.cjs
```
</verify>

---

### Task 4: IMPACT.md Rendering Tests

<action>
Add a `describe('renderImpactMarkdown', ...)` block with:

1. **`produces valid Markdown with YAML frontmatter`**:
   - Call `impact.renderImpactMarkdown({ change_id: 'test-change', tools_used: ['grep_search'], groups: [{ requirement_id: 'R1', requirement_title: 'Save cards', entries: [{ file: 'src/checkout.ts', evidence: 'Direct handler', source_ref: 'CHANGE-SPEC.md#R1', discovered_by: 'grep_search', confidence: 'confirmed' }] }], cross_cutting: [] })`
   - Assert output starts with `'---\n'`
   - Assert output contains `'change_id: test-change'`
   - Assert output contains `'phase: impact-discovery'`
   - Assert output contains `'status: draft'`

2. **`summary table has correct confidence counts`**:
   - Build data with 2 confirmed entries, 1 likely, 0 unknown across groups
   - Assert output contains `'| Confirmed | 2 |'`
   - Assert output contains `'| Likely | 1 |'`
   - Assert output contains `'| Unknown | 0 |'`

3. **`per-requirement sections with correct table columns`**:
   - Assert output contains `'## Impact: Requirement R1 — "Save cards"'`
   - Assert output contains `'| File | Evidence | Source Ref | Discovered By |'`
   - Assert output contains `'src/checkout.ts'`

4. **`empty groups produce None identified row`**:
   - Build data with an empty entries group
   - Assert output contains `'| None identified | — | — | — |'`

5. **`cross-cutting section is included`**:
   - Build data with cross_cutting entries
   - Assert output contains `'## Cross-cutting Impact'`

6. **`markdownTableCell escapes pipe characters`** (indirect test):
   - Build data where an evidence field contains `|` character
   - Assert the rendered output does NOT contain an unescaped `|` in the table cell position (check the evidence appears with `\|` escaping)
</action>

<read_first>
- tests/ndd-impact-discovery.test.cjs (current state)
- src/ndd-impact-discovery.cts (renderImpactMarkdown implementation, ImpactAnalysisData type)
- .planning/phases/03-impact-discovery/03-CONTEXT.md (Decision #6 and #8 for expected format)
</read_first>

<acceptance_criteria>
- 6 tests exist under `describe('renderImpactMarkdown')`
- All pass when running `node --test tests/ndd-impact-discovery.test.cjs`
- Tests verify: frontmatter, summary counts, per-requirement heading format, table columns, empty groups, cross-cutting section, pipe escaping
</acceptance_criteria>

<verify>
```bash
node --test --test-name-pattern='renderImpactMarkdown' tests/ndd-impact-discovery.test.cjs
```
</verify>

---

### Task 5: CHANGE-SPEC Requirement Extraction Tests

<action>
Add a `describe('extractRequirementSections', ...)` block with:

1. **`extracts requirements from Confirmed section`**:
   - Input: `'# Change Spec\n\n## Confirmed Source-Backed Requirements\n\n- First requirement\n- Second requirement\n\n## Open Questions\n'`
   - Assert result is `[{ id: 'R1', title: 'First requirement' }, { id: 'R2', title: 'Second requirement' }]`

2. **`returns empty array when no matching section`**:
   - Input: `'# Change Spec\n\n## Something Else\n\n- A bullet\n'`
   - Assert result is `[]`

3. **`returns empty array when section has no bullets`**:
   - Input: `'# Change Spec\n\n## Confirmed Source-Backed Requirements\n\nNo bullets here.\n'`
   - Assert result is `[]`

4. **`handles asterisk bullets`**:
   - Input: `'## Confirmed Source-Backed Requirements\n\n* Star bullet\n'`
   - Assert result is `[{ id: 'R1', title: 'Star bullet' }]`
</action>

<read_first>
- tests/ndd-impact-discovery.test.cjs (current state)
- src/ndd-impact-discovery.cts (extractRequirementSections implementation)
</read_first>

<acceptance_criteria>
- 4 tests exist under `describe('extractRequirementSections')`
- All pass
- Tests cover: happy path with bullets, no matching heading, no bullets in section, asterisk bullets
</acceptance_criteria>

<verify>
```bash
node --test --test-name-pattern='extractRequirementSections' tests/ndd-impact-discovery.test.cjs
```
</verify>

---

### Task 6: `runImpactDiscovery` Orchestrator Tests

<action>
Add a `describe('runImpactDiscovery', ...)` block with:

1. **`returns error for invalid change-id`**:
   - Call `impact.runImpactDiscovery({ projectRoot: tmpDir, changeId: '../escape' })`
   - Assert `result.ok === false`
   - Assert `result.error.code` is `'invalid_change_id'`

2. **`returns error for non-existent workspace`**:
   - Call `impact.runImpactDiscovery({ projectRoot: tmpDir, changeId: 'nonexistent-change' })`
   - Assert `result.ok === false`
   - Assert `result.error.code` is `'workspace_not_found'`

3. **`succeeds with valid workspace and creates IMPACT.md`**:
   - Use the pre-populated `test-change` workspace from beforeEach
   - Call `impact.runImpactDiscovery({ projectRoot: tmpDir, changeId: 'test-change' })`
   - Assert `result.ok === true`
   - Assert `result.change_id === 'test-change'`
   - Assert `fs.existsSync(path.join(tmpDir, '.planning', 'ndd', 'changes', 'test-change', 'IMPACT.md'))` is `true`

4. **`updates STATUS.json phase to impact`**:
   - After successful `runImpactDiscovery`, read STATUS.json
   - Assert `parsed.phase === 'impact'`
   - Assert `parsed.artifacts.impact === 'IMPACT.md'`
   - Assert `parsed.change_id === 'test-change'` (existing field preserved)
   - Assert `parsed.source_folder === '/tmp/sources'` (existing field preserved)

5. **`reports codebase map availability`**:
   - After successful run (codebase maps exist in fixture)
   - Assert `result.codebase_map_available === true`

6. **`reports codebase map unavailable when missing`**:
   - Remove `.planning/codebase/` directory from tmpDir
   - Run `impact.runImpactDiscovery({ projectRoot: tmpDir, changeId: 'test-change' })`
   - Assert `result.ok === true` (still succeeds — map is optional)
   - Assert `result.codebase_map_available === false`
</action>

<read_first>
- tests/ndd-impact-discovery.test.cjs (current state)
- src/ndd-impact-discovery.cts (runImpactDiscovery implementation)
- src/ndd-change-intake.cts (NddChangeStatus interface at lines 83–94)
</read_first>

<acceptance_criteria>
- 6 tests exist under `describe('runImpactDiscovery')`
- All pass
- Tests cover: invalid change-id, missing workspace, successful run, STATUS.json update, codebase map available, codebase map unavailable
- STATUS.json field preservation is explicitly tested (existing fields survive)
</acceptance_criteria>

<verify>
```bash
node --test --test-name-pattern='runImpactDiscovery' tests/ndd-impact-discovery.test.cjs
```
</verify>

---

### Task 7: CLI Integration Tests via `runGsdTools`

<action>
Add a `describe('CLI integration: ndd impact', ...)` block with:

1. **`returns error for missing change-id argument`**:
   - Call `runGsdTools('ndd impact', tmpDir)`
   - Assert `result.success === false`
   - Assert `result.error` matches `/Usage/`

2. **`returns error for non-existent change workspace`**:
   - Call `runGsdTools('ndd impact nonexistent', tmpDir)`
   - Assert `result.success === false`

3. **`returns structured result for valid workspace`**:
   - Call `runGsdTools('ndd impact test-change', tmpDir)`
   - Assert `result.success === true`
   - Parse `result.output` as JSON
   - Assert parsed `ok === true`
   - Assert parsed `change_id === 'test-change'`

Import `runGsdTools` from `./helpers.cjs`.
</action>

<read_first>
- tests/ndd-impact-discovery.test.cjs (current state)
- tests/helpers.cjs (runGsdTools function signature and return shape)
- tests/ndd-change-intake.test.cjs (for integration test pattern examples)
</read_first>

<acceptance_criteria>
- 3 tests exist under `describe('CLI integration: ndd impact')`
- Missing argument test asserts `success === false` and error contains `Usage`
- Non-existent workspace test asserts `success === false`
- Valid workspace test asserts `success === true` and output parses as JSON with `ok: true`
- All 3 pass
</acceptance_criteria>

<verify>
```bash
node --test --test-name-pattern='CLI integration' tests/ndd-impact-discovery.test.cjs
```
</verify>

---

### Task 8: Run Full Test Suite and Existing Tests

<action>
Run the complete test file:
```bash
node --test tests/ndd-impact-discovery.test.cjs
```

Then verify existing tests still pass:
```bash
node --test tests/ndd-change-intake.test.cjs
```

Fix any failures. Ensure no regressions in existing intake tests from the router modification.
</action>

<read_first>
- tests/ndd-impact-discovery.test.cjs (full test file)
- tests/ndd-change-intake.test.cjs (existing tests to verify no regression)
</read_first>

<acceptance_criteria>
- `node --test tests/ndd-impact-discovery.test.cjs` passes all tests (0 failures)
- `node --test tests/ndd-change-intake.test.cjs` passes all tests (0 failures, no regression)
- Total test count in impact test file is >= 26 (4 + 3 + 6 + 4 + 6 + 3)
</acceptance_criteria>

<verify>
```bash
node --test tests/ndd-impact-discovery.test.cjs 2>&1 | tail -5
node --test tests/ndd-change-intake.test.cjs 2>&1 | tail -5
```
</verify>

</tasks>

<verification>
After all tasks:
1. `node --test tests/ndd-impact-discovery.test.cjs` — all tests pass
2. `node --test tests/ndd-change-intake.test.cjs` — no regressions
3. Test coverage spans: config loading, map checking, IMPACT.md rendering, requirement extraction, orchestrator, CLI integration
4. UAT criteria from ROADMAP mapped:
   - SC1 (warn on missing map) → Task 6 tests #5 and #6
   - SC2 (IMPACT.md confidence levels) → Task 4 test #2
   - SC3 (evidence in entries) → Task 4 test #3
   - SC4 (feeds discuss-phase) → Structural (grouped by requirement, Tasks 4 and 5)
</verification>

<success_criteria>
- All impact discovery unit tests pass covering config, rendering, extraction, and orchestration
- CLI integration tests pass end-to-end through `runGsdTools`
- Existing NDD intake tests have zero regressions
- Missing codebase map behavior produces structured `codebase_map_available: false` in result (not crash)
- STATUS.json transitions are verified (phase, artifacts, field preservation)
</success_criteria>

<must_haves>
- [truth] `tests/ndd-impact-discovery.test.cjs` exists and has >= 26 individual test cases
- [truth] `loadCodebaseCapabilityConfig` has 4 tests: missing file, valid JSON, invalid JSON, missing tools key
- [truth] `checkCodebaseMapAvailability` has 3 tests: present, missing, empty
- [truth] `renderImpactMarkdown` has 6 tests covering frontmatter, counts, headings, columns, empty groups, escaping
- [truth] `extractRequirementSections` has 4 tests covering happy path, no section, no bullets, asterisk bullets
- [truth] `runImpactDiscovery` has 6 tests: invalid id, missing workspace, success, status update, map available, map unavailable
- [truth] CLI integration has 3 tests: missing arg, bad workspace, valid workspace
- [truth] `node --test tests/ndd-change-intake.test.cjs` still passes (no regression)
</must_haves>

## Artifacts this phase produces

### New Files
- `tests/ndd-impact-discovery.test.cjs` — Comprehensive test suite for impact discovery module

### Test Categories (26+ test cases)
| Category | Count | Tests |
|----------|-------|-------|
| `loadCodebaseCapabilityConfig` | 4 | missing file, valid JSON, invalid JSON, missing tools key |
| `checkCodebaseMapAvailability` | 3 | present dir, missing dir, empty dir |
| `renderImpactMarkdown` | 6 | frontmatter, summary counts, requirement headings, table columns, empty groups, pipe escaping |
| `extractRequirementSections` | 4 | happy path, no section, no bullets, asterisk bullets |
| `runImpactDiscovery` | 6 | invalid id, missing workspace, success + IMPACT.md creation, STATUS.json update, map available, map unavailable |
| CLI integration | 3 | missing arg, bad workspace, valid workspace |

### Test Fixtures
- Pre-populated change workspace with STATUS.json (phase: intake), CHANGE-SPEC.md (with requirement bullets)
- `.planning/codebase/ARCHITECTURE.md` sample codebase map
