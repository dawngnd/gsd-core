---
phase: 07-verification-ship-and-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
files_modified:
  - docs/COMMANDS.md
  - docs/CLI-TOOLS.md
  - docs/how-to/ndd-brownfield-change-workflow.md
  - skills/ndd-verify-work/SKILL.md
  - skills/ndd-ship/SKILL.md
  - tests/ndd-lifecycle.test.cjs
autonomous: true
requirements:
  - MNT-03
must_haves:
  truths:
    - "MNT-03/D-16: Tests cover router/workflow contracts and artifact generation for NDD verify, NDD ship, NDD `VERIFICATION.md`, ship context, `STATUS.json`, and command adapter content."
    - "MNT-03/D-17: Ship tests verify helper/router gates and generated context without creating a real PR."
    - "MNT-03/D-18: Documentation explains the full NDD lifecycle, artifacts, gates, and NDD adapter boundary over GSD core."
    - "D-15: Deterministic verify/ship logic lives in TypeScript helpers, while command Markdown stays focused on orchestration and delegation."
  prohibitions:
    - "Do not document NDD as a replacement for GSD Core."
    - "Do not claim NDD ship owns PR creation or review mechanics."
    - "Do not add tests that create a real PR or require network/GitHub authentication."
  artifacts:
    - "docs/how-to/ndd-brownfield-change-workflow.md"
    - "tests/ndd-lifecycle.test.cjs"
    - "skills/ndd-verify-work/SKILL.md"
    - "skills/ndd-ship/SKILL.md"
  key_links:
    - "Documentation flow: `ndd-change -> ndd-impact -> ndd-discuss-phase -> ndd-plan-phase -> ndd-execute-phase -> ndd-verify-work -> ndd-ship`."
    - "Lifecycle test fixture exercises intake through ship handoff with local files only."
---

<objective>
Add lifecycle documentation, command reference updates, and end-to-end workflow tests.

Purpose: satisfy MNT-03 and harden Phase 7 by documenting the complete NDD flow and proving command routing, artifact generation, and delegation contracts across the lifecycle.
Output: updated docs/skills and `tests/ndd-lifecycle.test.cjs`.
</objective>

<execution_context>
@/home/dangnd/code/github/gsd-core/.codex/gsd-core/workflows/execute-plan.md
@/home/dangnd/code/github/gsd-core/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-verification-ship-and-hardening/07-CONTEXT.md
@.planning/phases/07-verification-ship-and-hardening/07-01-SUMMARY.md
@.planning/phases/07-verification-ship-and-hardening/07-02-SUMMARY.md
@.planning/phases/06-execution-bridge/06-02-SUMMARY.md
@.planning/codebase/TESTING.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@docs/COMMANDS.md
@docs/CLI-TOOLS.md
@commands/ndd/change.md
@commands/ndd/impact.md
@commands/ndd/discuss-phase.md
@commands/ndd/plan-phase.md
@commands/ndd/execute-phase.md
@commands/ndd/verify-work.md
@commands/ndd/ship.md
@skills/ndd-verify-work/SKILL.md
@skills/ndd-ship/SKILL.md
</context>

## Artifacts this phase produces

- CLI commands: `gsd-tools ndd verify <change-id-or-phase>`, `gsd-tools ndd ship <change-id-or-phase>`, user-facing `ndd-verify-work <change-id-or-phase>`, user-facing `ndd-ship <change-id-or-phase>`.
- Helper modules/symbols: `src/ndd-verification.cts`, `prepareNddVerification`, `writeNddVerification`, `parseAcceptanceCriteria`, `resolveNddLinkedPhase`, `readGsdVerificationStatus`, `renderNddVerificationMarkdown`, `src/ndd-ship.cts`, `prepareNddShip`, `renderNddShipContext`, `markNddShipped`, `markNddShipBlocked`.
- Router outputs: `change_id`, `phase_id`, `phase_dir`, `phase_link_path`, `gsd_verification_status`, `gsd_verification_path`, `uat_path`, `verification_path`, `ship_context_path`, `gate_passed`, `requires_override`, `blocked_reason`.
- NDD change artifacts: `.planning/ndd/changes/<change-id>/VERIFICATION.md`, `.planning/ndd/changes/<change-id>/SHIP-CONTEXT.md`.
- `STATUS.json` fields: `verified`, `verification_path`, `verified_at`, `shipped`, `pr_url`, `pr_number`, `shipped_at`, `ship_status`, `ship_blocker`.
- Command adapters: `commands/ndd/verify-work.md`, `commands/ndd/ship.md`, generated/updated `skills/ndd-verify-work/SKILL.md`, `skills/ndd-ship/SKILL.md`.
- Tests/docs: `tests/ndd-verify-work.test.cjs`, `tests/ndd-ship.test.cjs`, `tests/ndd-lifecycle.test.cjs`, NDD command/lifecycle documentation.

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Verify completed work against `CHANGE-SPEC.md` | Covered by Plan 07-01 |
| GOAL | Reuse ship flow | Covered by Plan 07-02 |
| GOAL | Enough tests to maintain safely | Covered by Plan 07-03, with focused tests in 07-01 and 07-02 |
| REQ | VERF-01 | Covered by Plan 07-01 |
| REQ | VERF-02 | Covered by Plan 07-01 |
| REQ | SHIP-01 | Covered by Plan 07-02 |
| REQ | SHIP-02 | Covered by Plan 07-02 |
| REQ | MNT-03 | Covered by Plan 07-03 |
| CONTEXT | D-01 through D-09 | Covered by Plan 07-01 |
| CONTEXT | D-10 through D-14 | Covered by Plan 07-02 |
| CONTEXT | D-15 through D-18 | Covered across Plans 07-01, 07-02, and 07-03 |
| RESEARCH | No RESEARCH.md present; research disabled by init | Excluded |

<tasks>

<task type="auto">
  <name>Task 1: Update NDD lifecycle and command documentation</name>
  <files>docs/COMMANDS.md, docs/CLI-TOOLS.md, docs/how-to/ndd-brownfield-change-workflow.md, skills/ndd-verify-work/SKILL.md, skills/ndd-ship/SKILL.md</files>
  <read_first>
    @docs/COMMANDS.md
    @docs/CLI-TOOLS.md
    @commands/ndd/verify-work.md
    @commands/ndd/ship.md
    @skills/ndd-verify-work/SKILL.md
    @skills/ndd-ship/SKILL.md
    @scripts/gen-plugin-skills.cjs
    @.planning/phases/07-verification-ship-and-hardening/07-CONTEXT.md
  </read_first>
  <action>Update the public command references for the completed lifecycle per D-18. Add or update NDD sections in `docs/COMMANDS.md` and `docs/CLI-TOOLS.md` for `ndd verify` and `ndd ship`, including usage strings, required artifacts, gate behavior, generated artifacts, and delegation boundaries. Create `docs/how-to/ndd-brownfield-change-workflow.md` if no NDD lifecycle page exists; it must show the flow `ndd-change -> ndd-impact -> ndd-discuss-phase -> ndd-plan-phase -> ndd-execute-phase -> ndd-verify-work -> ndd-ship`, describe `.planning/ndd/changes/<change-id>/` artifacts, and state that NDD adapters reuse GSD core mechanics. Treat `skills/ndd-verify-work/SKILL.md` and `skills/ndd-ship/SKILL.md` as generated artifacts from `commands/ndd/verify-work.md` and `commands/ndd/ship.md`; after docs/command-adapter alignment, run `node scripts/gen-plugin-skills.cjs --write` to regenerate those skill bodies instead of hand-editing them.</action>
  <acceptance_criteria>
    - `docs/COMMANDS.md` documents user-facing `ndd-verify-work <change-id-or-phase>` and `ndd-ship <change-id-or-phase>`.
    - `docs/CLI-TOOLS.md` documents `gsd-tools ndd verify <change-id-or-phase>` and `gsd-tools ndd ship <change-id-or-phase>`.
    - Lifecycle doc names `CHANGE-SPEC.md`, `IMPACT.md`, NDD `VERIFICATION.md`, `SHIP-CONTEXT.md`, `STATUS.json`, and `phase-link.md`.
    - Skill bodies include the same helper invocations and canonical workflow delegation boundaries as the command adapters.
    - `skills/ndd-verify-work/SKILL.md` and `skills/ndd-ship/SKILL.md` are regenerated by `node scripts/gen-plugin-skills.cjs --write` and are current with command sources.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/gen-plugin-skills.cjs --check</automated>
    <automated>node -e "const fs=require('fs'); for (const file of ['docs/COMMANDS.md','docs/CLI-TOOLS.md','docs/how-to/ndd-brownfield-change-workflow.md','skills/ndd-verify-work/SKILL.md','skills/ndd-ship/SKILL.md']) { if (!fs.existsSync(file)) throw new Error(file + ' missing'); }"</automated>
  </verify>
  <done>NDD command references, lifecycle docs, and verify/ship skill bodies describe the complete flow, artifacts, gates, and GSD delegation boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add local end-to-end NDD lifecycle tests</name>
  <files>tests/ndd-lifecycle.test.cjs</files>
  <read_first>
    @tests/ndd-verify-work.test.cjs
    @tests/ndd-ship.test.cjs
    @tests/ndd-execution-bridge.test.cjs
    @commands/ndd/verify-work.md
    @commands/ndd/ship.md
    @.planning/codebase/TESTING.md
  </read_first>
  <behavior>
    - Test a synthetic `.planning/ndd/changes/<change-id>/` with approved `CHANGE-SPEC.md`, `IMPACT.md`, `STATUS.json`, `phase-link.md`, phase `VERIFICATION.md`, `UAT.md`, and summaries can run verify then ship helper/router paths and produce `VERIFICATION.md`, `SHIP-CONTEXT.md`, and status updates.
    - Test lifecycle command adapters mention every step in `ndd-change -> ndd-impact -> ndd-discuss-phase -> ndd-plan-phase -> ndd-execute-phase -> ndd-verify-work -> ndd-ship`.
    - Test docs and skills contain concrete artifact names and do not claim NDD owns GSD verifier, executor, or ship internals.
    - Test ship lifecycle path uses local fixture files only and never invokes `gh`, network, remote git, or real PR creation per D-17.
  </behavior>
  <action>Create `tests/ndd-lifecycle.test.cjs` using Node's built-in test runner and isolated temp project directories. Exercise compiled runtime modules from `gsd-core/bin/lib` or fresh `node .agents/gsd-core/bin/gsd-tools.cjs` subprocesses following existing NDD tests. The fixture must include a minimal change folder and linked phase folder with acceptance criteria, GSD passed verification, UAT evidence, phase-link metadata, and plan/summary files. Assert the end-to-end local sequence: `ndd verify` creates NDD `VERIFICATION.md` and marks status verified, then `ndd ship` creates `SHIP-CONTEXT.md` and returns the linked `phase_id` for canonical ship. Keep assertions deterministic and filesystem-local.</action>
  <acceptance_criteria>
    - `tests/ndd-lifecycle.test.cjs` covers NDD artifact generation paths from verification through ship context.
    - Tests assert `STATUS.json.verified`, `verification_path`, `verified_at`, and `ship_context_path` behavior.
    - Tests assert command/skill/docs content for D-15 through D-18.
    - Tests do not require network, GitHub CLI authentication, a real remote, or a real PR.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-tests.cjs --files "tests/ndd-lifecycle.test.cjs,tests/ndd-verify-work.test.cjs,tests/ndd-ship.test.cjs"</automated>
  </verify>
  <done>Local lifecycle tests prove verify-to-ship artifact generation and status behavior without network, GitHub auth, or real PR creation.</done>
</task>

<task type="auto">
  <name>Task 3: Run hardening verification for NDD lifecycle surface</name>
  <files>tests/ndd-lifecycle.test.cjs, docs/COMMANDS.md, docs/CLI-TOOLS.md, skills/ndd-verify-work/SKILL.md, skills/ndd-ship/SKILL.md</files>
  <read_first>
    @package.json
    @scripts/run-tests.cjs
    @eslint.config.mjs
    @tests/ndd-lifecycle.test.cjs
    @tests/ndd-verify-work.test.cjs
    @tests/ndd-ship.test.cjs
    @scripts/gen-plugin-skills.cjs
  </read_first>
  <action>Run the focused lifecycle, verify, and ship tests together after `tests/ndd-lifecycle.test.cjs` exists from Task 2, verify generated skill freshness with `node scripts/gen-plugin-skills.cjs --check`, then run TypeScript build and lint for the changed TypeScript/Markdown/test surface. If generated CommonJS output is produced by the build, include it in the execution summary and leave it in the working tree if the repository tracks that generated path. Fix failures by changing the smallest relevant source, test, command, generated skill, or docs file from this plan and prior Phase 7 plans; regenerate generated skills with `node scripts/gen-plugin-skills.cjs --write` after any command adapter change.</action>
  <acceptance_criteria>
    - Focused tests for `ndd-lifecycle`, `ndd-verify-work`, and `ndd-ship` pass in one command.
    - `node scripts/gen-plugin-skills.cjs --check` passes after any generated NDD skill updates.
    - `npx tsc --project tsconfig.build.json` passes.
    - `npm run lint -- --quiet` or the repository's supported lint command passes for changed files; if lint command does not support changed-file filtering, run the documented lint command and report failures.
    - No test creates a real PR or requires network access.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-tests.cjs --files "tests/ndd-lifecycle.test.cjs,tests/ndd-verify-work.test.cjs,tests/ndd-ship.test.cjs"</automated>
    <automated>node scripts/gen-plugin-skills.cjs --check</automated>
    <automated>npx tsc --project tsconfig.build.json</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>Focused NDD lifecycle tests, TypeScript build, and lint pass or any remaining tool limitation is recorded in the summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs/skills -> user actions | Documentation and skill text instruct users and agents how to run verification/ship gates. |
| Tests -> local filesystem | Lifecycle tests create synthetic `.planning/` and phase artifacts. |
| Test process -> external tools | Ship-related tests must not call network, `gh`, or real PR creation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03-01 | Spoofing | Docs/skills command examples | medium | mitigate | Use exact command names and helper invocations implemented in Plans 07-01 and 07-02. |
| T-07-03-02 | Tampering | Lifecycle test fixture writes | medium | mitigate | Use isolated temp project directories and existing path-safe helpers; do not touch the user's real `.planning/ndd/changes/`. |
| T-07-03-03 | Repudiation | Lifecycle documentation | low | mitigate | Document concrete artifacts and status fields generated by verification and ship. |
| T-07-03-04 | Information Disclosure | Docs/ship context examples | low | accept | Examples use synthetic artifact names and do not embed real source documents. |
| T-07-03-05 | Elevation of Privilege | Ship tests | high | mitigate | Tests assert helper/router output and generated context only; no `gh`, remote git, network, or real PR calls per D-17. |
</threat_model>

<verification>
Run the focused lifecycle hardening checks:
- `node scripts/run-tests.cjs --files "tests/ndd-lifecycle.test.cjs,tests/ndd-verify-work.test.cjs,tests/ndd-ship.test.cjs"`
- `node scripts/gen-plugin-skills.cjs --check`
- `npx tsc --project tsconfig.build.json`
- `npm run lint`
</verification>

<success_criteria>
- NDD lifecycle docs describe the complete change flow through ship.
- NDD verify/ship skills mirror command adapter behavior.
- Lifecycle tests prove artifact generation, status updates, and command/skill/docs contracts.
- No test requires network access or creates a real PR.
</success_criteria>

<output>
Create `.planning/phases/07-verification-ship-and-hardening/07-03-SUMMARY.md` when done.
</output>
