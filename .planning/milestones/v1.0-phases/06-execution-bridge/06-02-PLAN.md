---
phase: 06-execution-bridge
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - tests/ndd-execution-bridge.test.cjs
requirements:
  - EXEC-01
  - EXEC-02
  - EXEC-03
autonomous: true
must_haves:
  truths:
    - "EXEC-01: Tests prove gate enforcement — execute fails when phase-link.md is absent and when no *-PLAN.md files exist."
    - "EXEC-01: Tests prove dual-path resolution — change-id resolves via phase-link.md, phase-id resolves via reverse scan."
    - "EXEC-02: Tests prove JSON output contains change_id, phase_id, phase_dir, plan_files, phase_link_path fields."
    - "EXEC-02: Tests prove ambiguity error when multiple changes link to the same phase-id."
    - "EXEC-03: Tests prove execute-phase.md references canonical GSD execute-phase and does not define an independent executor flow."
    - "D-01: Tests verify lightweight gate — phase-link.md existence and PLAN.md file presence are enforced."
    - "D-03: Tests verify dual-path resolution accepts both change-id and phase-id inputs."
    - "D-04: Tests verify change-id forward lookup via phase-link.md."
    - "D-05: Tests verify phase-id reverse scan of changes/*/phase-link.md."
    - "D-06: Tests verify format auto-detection distinguishes numeric phase-id from kebab-case change-id."
    - "D-09: Tests verify JSON output shape matches specified fields."
    - "D-11: Tests verify workflow adapter calls gsd-tools then delegates to gsd-execute-phase."
  artifacts:
    - "tests/ndd-execution-bridge.test.cjs covers gate enforcement, dual-path resolution, ambiguity handling, and workflow adapter content assertions."
  key_links:
    - "Tests use runGsdTools('ndd execute ...') CLI-level testing pattern from tests/helpers.cjs."
    - "Tests reuse buildChangeWorkspace and buildPhaseFixture fixture patterns from ndd-plan-bridge.test.cjs."
    - "Workflow content tests assert non-duplication of GSD executor internals."
---

<objective>
Add traceability verification, status update tests, and comprehensive test coverage for the execution bridge.

Purpose: Prove that the `ndd execute` subcommand enforces gates correctly, resolves both change-id and phase-id inputs, handles ambiguity, and that the workflow adapter stays thin.
Output: A focused `tests/ndd-execution-bridge.test.cjs` test file covering EXEC-01, EXEC-02, and EXEC-03 requirements.
</objective>

<execution_context>
@/home/dangnd/code/github/gsd-core/.codex/gsd-core/workflows/execute-plan.md
@/home/dangnd/code/github/gsd-core/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-execution-bridge/06-CONTEXT.md
@.planning/phases/06-execution-bridge/06-RESEARCH.md
@.planning/phases/06-execution-bridge/06-PATTERNS.md
@.planning/phases/06-execution-bridge/06-01-SUMMARY.md
@src/ndd-command-router.cts
@tests/ndd-plan-bridge.test.cjs
@tests/helpers.cjs
@commands/ndd/execute-phase.md
</context>

## Artifacts this plan produces

- `tests/ndd-execution-bridge.test.cjs` — test file with requirement-linked describe blocks:
  - `describe('EXEC-01: Execute gate enforcement')` — tests that verify gate behavior:
    - `test('execute fails when phase-link.md is missing')` — runGsdTools returns non-zero with `Phase-link metadata not found`
    - `test('execute fails when no plan files in phase dir')` — runGsdTools returns non-zero with `No plan files found`
    - `test('execute succeeds with valid phase-link.md and plan files')` — runGsdTools returns zero with JSON containing `ok: true`
  - `describe('EXEC-01: Dual-path resolution')` — tests for both input formats:
    - `test('change-id input resolves via phase-link.md')` — valid change-id returns matching phase_id and change_id
    - `test('phase-id input resolves via reverse scan')` — numeric phase-id returns matching change_id and phase_id
    - `test('phase-id with no linked change returns error')` — runGsdTools returns non-zero with `No NDD change found`
    - `test('phase-id with multiple linked changes returns ambiguity error')` — two change workspaces link to same phase, returns non-zero with `Multiple NDD changes`
    - `test('missing arg returns usage error')` — runGsdTools returns non-zero with `Usage:`
  - `describe('EXEC-02: Traceability output fields')` — tests for JSON shape:
    - `test('JSON output includes change_id, phase_id, phase_dir, plan_files, phase_link_path')` — assert all fields present and correctly typed
    - `test('plan_files array lists actual PLAN.md filenames')` — assert plan filenames match created fixture files
  - `describe('EXEC-03: Workflow adapter content')` — static content assertions:
    - `test('execute-phase.md references canonical GSD execute-phase')` — assert content matches `/gsd-execute-phase/`
    - `test('execute-phase.md references gsd-tools ndd execute')` — assert content matches `/gsd-tools ndd execute/`
    - `test('execute-phase.md does not define independent executor flow')` — assert content matches `/Do not copy or inline executor internals/i`
    - `test('execute-phase.md does not update STATUS.json')` — assert content does NOT match `/writeStatus|STATUS\.json.*update|STATUS\.json.*write/i`
  - New fixture helper:
    - `buildPhaseLinkFixture(tmpDir, changeId, phaseId, phaseDir, planFiles)` — writes a `phase-link.md` file at `.planning/ndd/changes/<changeId>/phase-link.md` using the plain markdown list format matching the output of `writePhaseLink`

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Execution traceable to NDD change id | Covered by EXEC-02 output field tests |
| REQ | EXEC-01 reuse GSD execution mechanics | Covered by gate enforcement and resolution tests proving delegation path exists |
| REQ | EXEC-02 execution summaries traceable | Covered by JSON output field assertions |
| REQ | EXEC-03 no fork of GSD executor | Covered by workflow content assertions |
| CONTEXT | D-01 lightweight gate | Covered by gate enforcement tests (phase-link + PLAN.md) |
| CONTEXT | D-03/D-04/D-05 dual-path resolution | Covered by change-id and phase-id path tests |
| CONTEXT | D-05 ambiguity handling | Covered by multiple-change-same-phase test |
| CONTEXT | D-06 format detection | Covered implicitly by dual-path test success |
| CONTEXT | D-07 no post-execution NDD metadata updates | Covered by workflow content assertion (no writeStatus) |
| RESEARCH | Reverse lookup scan pattern | Covered by phase-id resolution test |
| PATTERNS | CLI-level test pattern from ndd-plan-bridge.test.cjs | Covered by runGsdTools usage in all CLI tests |

<tasks>

<task type="auto">
  <name>Task 1: Create execution bridge test file with gate enforcement tests</name>
  <files>tests/ndd-execution-bridge.test.cjs</files>
  <read_first>
    - tests/ndd-plan-bridge.test.cjs
    - tests/helpers.cjs
    - src/ndd-command-router.cts
    - commands/ndd/execute-phase.md
    - .planning/phases/06-execution-bridge/06-CONTEXT.md
    - .planning/phases/06-execution-bridge/06-RESEARCH.md
    - .planning/phases/06-execution-bridge/06-PATTERNS.md
  </read_first>
  <action>
    Create `tests/ndd-execution-bridge.test.cjs`. Follow the exact import and infrastructure pattern from `tests/ndd-plan-bridge.test.cjs`:

    Imports:
    - `const { describe, test, beforeEach, afterEach } = require('node:test');`
    - `const assert = require('node:assert/strict');`
    - `const fs = require('node:fs');`
    - `const path = require('node:path');`
    - `const { createTempDir, cleanup, runGsdTools } = require('./helpers.cjs');`

    Add an `assertCommandOk(result)` helper identical to the one in ndd-plan-bridge.test.cjs: `assert.equal(result.exitCode, 0, result.error || result.output)`.

    Add fixture helpers:

    1. `buildPhaseLinkFixture(tmpDir, changeId, phaseId, phaseDir, planFiles)` — creates `.planning/ndd/changes/<changeId>/phase-link.md` using exact plain markdown list format:
       ```
       # NDD Phase Link: <changeId>
       
       - Change id: <changeId>
       - GSD phase id: <phaseId>
       - GSD phase directory: <phaseDir>
       - Linked at: 2026-01-01T00:00:00.000Z
       
       ## Plan Files
       
       - <planFile1>
       - <planFile2>
       
       ## Source Artifacts
       
       - STATUS.json: .planning/ndd/changes/<changeId>/STATUS.json
       - CHANGE-SPEC.md: .planning/ndd/changes/<changeId>/CHANGE-SPEC.md
       ```

    2. Reuse the `buildPhaseFixture(tmpDir, phaseNum, phaseName)` pattern from ndd-plan-bridge.test.cjs to create phase directories with ROADMAP.md. Also write a plan file (e.g., `<phaseNum>-01-PLAN.md`) in the phase directory for gate-pass scenarios.

    Implement describe blocks linked to requirements:

    **`describe('EXEC-01: Execute gate enforcement')`:**
    - Each test uses `beforeEach(() => { tmpDir = createTempDir('gsd-ndd-exec-bridge-'); })` and `afterEach(() => { cleanup(tmpDir); })`.
    - `test('execute fails when phase-link.md is missing')`: Create change workspace directory but NO phase-link.md. Run `runGsdTools('ndd execute test-change', tmpDir)`. Assert `result.success === false`. Assert `result.error` matches `/Phase-link metadata not found/`.
    - `test('execute fails when no plan files exist in phase dir')`: Create change workspace, write phase-link.md pointing to a valid phase dir, create the phase dir via buildPhaseFixture, but do NOT write any `*-PLAN.md` file. Run `runGsdTools('ndd execute test-change', tmpDir)`. Assert `result.success === false`. Assert `result.error` matches `/No plan files found/`.
    - `test('execute succeeds with valid phase-link.md and plan files')`: Create change workspace, buildPhaseFixture with a plan file, buildPhaseLinkFixture pointing to that phase. Run `runGsdTools('ndd execute test-change', tmpDir)`. Call `assertCommandOk(result)`. Parse JSON from `result.output`. Assert `parsed.ok === true`, `parsed.change_id === 'test-change'`, `parsed.gate_passed === true`.

    **`describe('EXEC-01: Dual-path resolution')`:**
    - `test('change-id input resolves via phase-link.md')`: Full fixture, run `runGsdTools('ndd execute test-change', tmpDir)`. Assert parsed JSON `phase_id` matches the fixture phase id.
    - `test('phase-id input resolves via reverse scan')`: Full fixture, run `runGsdTools('ndd execute 06', tmpDir)` (numeric input). Assert parsed JSON `change_id === 'test-change'`.
    - `test('phase-id with no linked change returns error')`: Create phase dir but no change workspace. Run `runGsdTools('ndd execute 06', tmpDir)`. Assert failure with `/No NDD change found/`.
    - `test('phase-id with multiple linked changes returns ambiguity error')`: Create two change workspaces (`test-change-a` and `test-change-b`) both with phase-link.md pointing to phase `06`. Run `runGsdTools('ndd execute 06', tmpDir)`. Assert failure with `/Multiple NDD changes/`.
    - `test('missing arg returns usage error')`: Run `runGsdTools('ndd execute', tmpDir)`. Assert failure with `/Usage/`.

    **`describe('EXEC-02: Traceability output fields')`:**
    - `test('JSON output includes all required traceability fields')`: Full fixture, parse JSON, assert presence and correct values for `change_id`, `phase_id`, `phase_dir`, `plan_files` (array), `phase_link_path` (string containing `phase-link.md`).
    - `test('plan_files array lists actual PLAN.md filenames')`: Create 2 plan files in phase dir, update fixture. Assert `parsed.plan_files` has length 2 and includes both filenames.

    **`describe('EXEC-03: Workflow adapter content')`:**
    - No temp dirs needed — these are static file content tests.
    - `const executePhasePath = path.join(__dirname, '..', 'commands', 'ndd', 'execute-phase.md');`
    - `test('execute-phase.md references canonical GSD execute-phase')`: Read file, `assert.match(content, /gsd-execute-phase/)`.
    - `test('execute-phase.md references gsd-tools ndd execute')`: `assert.match(content, /gsd-tools ndd execute/)`.
    - `test('execute-phase.md does not define independent executor flow')`: `assert.match(content, /Do not copy or inline executor internals/i)`.
    - `test('execute-phase.md does not update STATUS.json')`: `assert.doesNotMatch(content, /writeStatus/i)`. `assert.doesNotMatch(content, /STATUS\.json.*update/i)`.
  </action>
  <acceptance_criteria>
    - `tests/ndd-execution-bridge.test.cjs` exists and uses `node:test` runner.
    - Running `node --test tests/ndd-execution-bridge.test.cjs` completes with zero failures.
    - The test file contains at least 4 `describe` blocks linked to EXEC-01, EXEC-02, EXEC-03.
    - Gate enforcement tests cover: missing phase-link.md, no plan files, and happy path.
    - Dual-path tests cover: change-id path, phase-id path, no-match error, ambiguity error, and missing arg.
    - Traceability tests assert JSON output shape with specific field names.
    - Workflow content tests assert references to canonical GSD execute-phase and non-duplication statement.
    - No test imports GSD executor internals — all CLI-level tests use `runGsdTools`.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/ndd-execution-bridge.test.cjs</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture content | Tests write controlled phase-link.md content to verify regex parsing behavior. |
| CLI invocation | Tests invoke gsd-tools through the same runGsdTools helper used by existing NDD tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Tampering | Test fixtures | low | accept | Fixtures use hardcoded safe values matching the writePhaseLink output format. |
| T-06-06 | Information Disclosure | Temp directories | low | mitigate | Tests use createTempDir/cleanup pattern ensuring temp dirs are removed after each test. |
</threat_model>

<verification>
Overall check: `node --test tests/ndd-execution-bridge.test.cjs` passes with zero failures.
</verification>

<success_criteria>
Plan 06-02 is complete when `tests/ndd-execution-bridge.test.cjs` passes all tests covering gate enforcement, dual-path resolution, traceability output, ambiguity handling, and workflow adapter content assertions for the execution bridge.
</success_criteria>

<output>
Create `.planning/phases/06-execution-bridge/06-02-SUMMARY.md` when done.
</output>
