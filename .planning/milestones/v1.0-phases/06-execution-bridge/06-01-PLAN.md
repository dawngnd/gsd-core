---
phase: 06-execution-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ndd-command-router.cts
  - gsd-core/bin/lib/ndd-command-router.cjs
  - commands/ndd/execute-phase.md
requirements:
  - EXEC-01
  - EXEC-02
  - EXEC-03
autonomous: true
must_haves:
  truths:
    - "EXEC-01: `gsd-tools ndd execute <change-id>` resolves a change-id to the linked GSD phase via phase-link.md and returns execution-ready JSON."
    - "EXEC-01: `gsd-tools ndd execute <phase-id>` resolves a phase-id to the owning NDD change via reverse scan of changes/*/phase-link.md."
    - "EXEC-01: Gate fails when phase-link.md is absent or when no *-PLAN.md files exist in the target phase directory."
    - "EXEC-02: The JSON output contains change_id, phase_id, phase_dir, plan_files[], phase_link_path so downstream workflow and summaries can trace back to the NDD change."
    - "EXEC-03: commands/ndd/execute-phase.md invokes canonical gsd-execute-phase and does not duplicate GSD executor internals."
    - "EXEC-03: The workflow adapter passes --wave and --gaps-only flags through to gsd-execute-phase verbatim."
    - "D-01: Gate is lightweight — checks phase-link.md existence and at least 1 *-PLAN.md in target phase dir."
    - "D-03: Dual-path input — supports both change-id and phase-id as arguments."
    - "D-04: Change-id resolves via phase-link.md in .planning/ndd/changes/<change-id>/phase-link.md."
    - "D-05: Phase-id resolves via reverse scan of .planning/ndd/changes/*/phase-link.md."
    - "D-06: Auto format detection — isPhaseIdInput distinguishes numeric phase-id from kebab-case change-id."
    - "D-09: Router JSON output includes change_id, phase_id, phase_dir, plan_files, phase_link_path."
    - "D-10: No new TS helper file — resolve logic inline in router, reuses existing helpers."
    - "D-11: Workflow adapter calls gsd-tools ndd execute then delegates to gsd-execute-phase."
    - "D-02: [informational] Gate check lives in workflow adapter, not in TS helper."
    - "D-08: [informational] Traceability path already sufficient via phase-link.md → phase-dir → SUMMARY.md."
  artifacts:
    - "src/ndd-command-router.cts has an `execute` subcommand branch with dual-path resolution, gate check, and structured JSON output."
    - "gsd-core/bin/lib/ndd-command-router.cjs is emitted by the TypeScript build and committed with the source change."
    - "commands/ndd/execute-phase.md is a complete 4-step workflow adapter."
  key_links:
    - "Router reuses resolveChangeWorkspace and validateChangeId from src/ndd-change-intake.cts."
    - "Router reuses locateCurrentPhase from its own internal helpers."
    - "Workflow adapter references canonical .agents/gsd-core/workflows/execute-phase.md."
    - "phase-link.md is parsed inline using regex matching the plain markdown list format written by writePhaseLink in src/ndd-plan-bridge.cts."
---

<objective>
Implement the `ndd execute` router subcommand and the `ndd:execute-phase` workflow adapter.

Purpose: Users should execute an NDD-planned change through ordinary GSD mechanics while NDD handles phase-link resolution, gate checking, and traceability.
Output: A router extension in `src/ndd-command-router.cts` with dual-path resolution and a full `commands/ndd/execute-phase.md` workflow adapter that delegates to canonical GSD execute-phase.
</objective>

<execution_context>
@/home/dangnd/code/github/gsd-core/.codex/gsd-core/workflows/execute-plan.md
@/home/dangnd/code/github/gsd-core/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-execution-bridge/06-CONTEXT.md
@.planning/phases/06-execution-bridge/06-RESEARCH.md
@.planning/phases/06-execution-bridge/06-PATTERNS.md
@.planning/phases/05-planning-bridge/05-01-SUMMARY.md
@.planning/phases/05-planning-bridge/05-02-SUMMARY.md
@src/ndd-command-router.cts
@src/ndd-plan-bridge.cts
@src/ndd-change-intake.cts
@commands/ndd/execute-phase.md
@commands/ndd/plan-phase.md
</context>

## Artifacts this plan produces

- `src/ndd-command-router.cts` — extended with:
  - `execute` subcommand branch (after `plan-link`, before `else` fallback)
  - Inline helper `readPhaseLinkPhaseId(content: string): string | null` — regex `/^- GSD phase id:\s*(.+)$/m`
  - Inline helper `readPhaseLinkChangeId(content: string): string | null` — regex `/^- Change id:\s*(.+)$/m`
  - Inline helper `readPhaseLinkPhaseDir(content: string): string | null` — regex `/^- GSD phase directory:\s*(.+)$/m`
  - Inline helper `readPhaseLinkPlanFiles(content: string): string[]` — splits `## Plan Files` section
  - Inline helper `isPhaseIdInput(input: string): boolean` — matches `/^\d+$/`
  - Inline helper `resolveExecutionContext(cwd, input)` — dual-path resolution returning `{ ok, change_id, phase_id, phase_dir, plan_files, phase_link_path }` or error
  - Updated doc-comment at file top adding `execute` entry
  - Updated unknown-subcommand error message adding `execute` to the Available list
- `gsd-core/bin/lib/ndd-command-router.cjs` — generated by `npx tsc --project tsconfig.build.json`
- `commands/ndd/execute-phase.md` — complete 4-step workflow adapter:
  - Step 1: Parse arguments (`<change-id-or-phase> [--wave N] [--gaps-only]`)
  - Step 2: Gate via `gsd-tools ndd execute <id>` (hard gate on non-zero exit)
  - Step 3: Delegate to `gsd-execute-phase <phase-id>` with flag passthrough
  - Step 4: Report result with NDD traceability

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | `ndd-execute-phase` reuses GSD execution while preserving traceability | Covered by workflow adapter delegation to canonical execute-phase |
| REQ | EXEC-01 reuse GSD execution mechanics and subagent coordination | Covered by workflow invoking `gsd-execute-phase <phase-id>` |
| REQ | EXEC-02 execution summaries traceable to NDD change id | Covered by router JSON output containing change_id, phase_link_path; traceability via phase-link.md |
| REQ | EXEC-03 no fork of GSD executor core | Covered by workflow explicitly stating "Do not duplicate GSD executor internals" |
| CONTEXT | D-01 lightweight gate: phase-link.md exists + at least 1 PLAN.md | Covered by gate check in execute subcommand |
| CONTEXT | D-03/D-04/D-05 dual-path resolution | Covered by isPhaseIdInput + forward/reverse lookup in resolveExecutionContext |
| CONTEXT | D-06 change-id vs phase-id format detection | Covered by isPhaseIdInput heuristic |
| CONTEXT | D-07 no post-execution NDD metadata updates | Covered by workflow having no post-delegation write step |
| CONTEXT | D-09 router returns JSON with change_id, phase_id, phase_dir, plan_files, phase_link_path | Covered by output shape |
| CONTEXT | D-10 no new TS helper file | Covered by inline logic in router |
| CONTEXT | D-11 workflow calls gsd-tools ndd execute then gsd-execute-phase | Covered by workflow Steps 2–3 |
| RESEARCH | phase-link.md uses plain markdown list, not YAML frontmatter | Covered by regex-based parsers |
| PATTERNS | plan-link branch pattern reuse | Covered by same arg-parse/output/error style |

<tasks>

<task type="auto">
  <name>Task 1: Add `execute` subcommand to NDD command router</name>
  <files>src/ndd-command-router.cts</files>
  <read_first>
    - src/ndd-command-router.cts
    - src/ndd-plan-bridge.cts
    - src/ndd-change-intake.cts
    - .planning/phases/06-execution-bridge/06-CONTEXT.md
    - .planning/phases/06-execution-bridge/06-RESEARCH.md
    - .planning/phases/06-execution-bridge/06-PATTERNS.md
  </read_first>
  <action>
    Add the `execute` subcommand to `src/ndd-command-router.cts`. Insert the new branch after the `plan-link` branch (after L417) and before the `else` fallback (L418). Follow the existing subcommand dispatch pattern used by `plan-link` and `plan` branches.

    Add these inline helper functions (private to the module, placed near the existing `toPosixPath` and `locateCurrentPhase` helpers around L48-84):

    1. `readPhaseLinkPhaseId(content: string): string | null` — extracts value from line matching `/^- GSD phase id:\s*(.+)$/m`, returns trimmed match or null.
    2. `readPhaseLinkChangeId(content: string): string | null` — extracts value from line matching `/^- Change id:\s*(.+)$/m`.
    3. `readPhaseLinkPhaseDir(content: string): string | null` — extracts value from line matching `/^- GSD phase directory:\s*(.+)$/m`.
    4. `readPhaseLinkPlanFiles(content: string): string[]` — splits content on `## Plan Files`, takes lines between that heading and the next `##`, extracts lines matching `/^- (.+-PLAN\.md.*)$/`, returns trimmed array.
    5. `isPhaseIdInput(input: string): boolean` — returns true when input matches `/^\d+$/` (pure numeric like `06`, `6`).

    Implement the `execute` branch logic:

    - Parse single required arg: `const inputId = args[2]`. Reject missing or flag-prefixed arg with `error('Usage: gsd-tools ndd execute <change-id-or-phase>', ERROR_REASON.USAGE)`.
    - Dual-path detection: call `isPhaseIdInput(inputId)` to choose the resolution path.
    - **Change-id path** (inputId is not a phase-id):
      - Call `intake.resolveChangeWorkspace(cwd, inputId)`. On failure, report via `error(...)`.
      - Read `phase-link.md` from `path.join(workspace.workspace_dir, 'phase-link.md')`. If missing, error with message: `Phase-link metadata not found for change '${inputId}'. Run ndd-plan-phase first.`
      - Parse content with `readPhaseLinkPhaseId`, `readPhaseLinkChangeId`, `readPhaseLinkPhaseDir`, `readPhaseLinkPlanFiles`.
      - If `readPhaseLinkPhaseId` returns null, error: `phase-link.md for change '${inputId}' does not contain a GSD phase id.`
    - **Phase-id path** (inputId is a phase-id):
      - Scan `.planning/ndd/changes/` with `fs.readdirSync`. For each subdirectory, check if `phase-link.md` exists.
      - Parse each `phase-link.md` content. Collect entries where `readPhaseLinkPhaseId(content)` matches `inputId`.
      - If zero matches, error: `No NDD change found linked to phase '${inputId}'.`
      - If multiple matches, error: `Multiple NDD changes linked to phase '${inputId}': ${changeIds.join(', ')}. Specify a change-id instead.`
      - Use the single match's parsed values.
    - **Gate check** (both paths converge):
      - Locate the phase directory with `locateCurrentPhase(cwd, parsedPhaseId)`. If null, error: `Target GSD phase '${parsedPhaseId}' was not found under .planning/phases/.`
      - Scan for `*-PLAN.md` files in the phase dir using `fs.readdirSync(absPhaseDir).filter(f => f.endsWith('-PLAN.md'))`. If empty, error: `No plan files found in phase directory '${phaseDir}'. Run ndd-plan-phase first.`
    - **Output** on success: call `output({ ok: true, change_id, phase_id, phase_dir, plan_files, phase_link_path: toPosixPath(relative path to phase-link.md), gate_passed: true }, raw)`.

    Update the doc-comment at file top (L1-13) to add: `* - \`gsd-tools ndd execute <change-id-or-phase>\` → execution bridge gate and resolution`.

    Update the unknown-subcommand error message (L418-419) to include `execute` in the Available list: `'Unknown ndd subcommand. Available: change, impact, discuss, discuss-update, discuss-check, plan, plan-context, plan-link, execute'`.

    Ensure `npx tsc --project tsconfig.build.json` emits `gsd-core/bin/lib/ndd-command-router.cjs`; commit the generated runtime output with the source file.
  </action>
  <acceptance_criteria>
    - `gsd-tools ndd execute <change-id>` with a valid change workspace + phase-link.md + plan files returns JSON with `ok: true`, `change_id`, `phase_id`, `phase_dir`, `plan_files` array, `phase_link_path`, `gate_passed: true`.
    - `gsd-tools ndd execute <phase-id>` with a matching phase-link.md in one change workspace returns the same JSON shape.
    - `gsd-tools ndd execute` (no arg) returns non-zero exit with `Usage:` in stderr.
    - `gsd-tools ndd execute <change-id>` when phase-link.md is missing returns non-zero exit with message containing `Phase-link metadata not found`.
    - `gsd-tools ndd execute <change-id>` when phase dir has no `*-PLAN.md` files returns non-zero exit with message containing `No plan files found`.
    - `gsd-tools ndd execute <phase-id>` when no change links to that phase returns non-zero exit with `No NDD change found linked to phase`.
    - `gsd-tools ndd execute <phase-id>` when multiple changes link to same phase returns non-zero exit with `Multiple NDD changes linked to phase`.
    - The unknown-subcommand error message includes `execute`.
    - The file top doc-comment mentions `execute`.
    - `npx tsc --project tsconfig.build.json` succeeds.
    - `npx eslint src/ndd-command-router.cts` passes.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --project tsconfig.build.json</automated>
    <automated>npx eslint src/ndd-command-router.cts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Replace placeholder `ndd:execute-phase` with full workflow adapter</name>
  <files>commands/ndd/execute-phase.md</files>
  <read_first>
    - commands/ndd/execute-phase.md
    - commands/ndd/plan-phase.md
    - .agents/gsd-core/workflows/execute-phase.md
    - .agents/gsd-core/references/agent-contracts.md
    - .planning/phases/06-execution-bridge/06-CONTEXT.md
    - .planning/phases/06-execution-bridge/06-PATTERNS.md
  </read_first>
  <action>
    Rewrite `commands/ndd/execute-phase.md` from its current 39-line stub into a complete workflow adapter. Keep the existing frontmatter (name: `ndd:execute-phase`, description, argument-hint: `<change-id-or-phase> [--wave N]`, effort: max, allowed-tools including TodoWrite, requires: []).

    Rewrite the `<objective>` section to: "Bridge NDD phase-link metadata into canonical GSD execute-phase without copying executor internals. NDD owns the execution gate and change-id resolution. GSD owns wave-based plan execution, subagent coordination, summaries, and verification."

    Update `<execution_context>` to reference:
    - `@.agents/gsd-core/workflows/execute-phase.md`
    - `@gsd-core/workflows/execute-phase.md`
    - `@.agents/gsd-core/references/agent-contracts.md`
    - `@.planning/ndd/changes/`

    Replace `<process>` with a 4-step executable workflow:

    **Step 1: Parse arguments** — Parse `$ARGUMENTS` as `<change-id-or-phase> [--wave N] [--gaps-only]`. Extract `INPUT_ID` (first positional token), `WAVE_FLAG` (value after `--wave`), and `GAPS_ONLY_FLAG` (boolean). If `INPUT_ID` is missing, stop with usage message: `Usage: ndd-execute-phase <change-id-or-phase> [--wave N] [--gaps-only]`.

    **Step 2: Gate and resolve the NDD change** — Run `gsd-tools ndd execute <INPUT_ID>`. This is a hard gate. If the command exits non-zero, stop. Common blocking cases: phase-link.md missing (run ndd-plan-phase first), no plan files in target phase, no change linked to supplied phase-id, multiple changes linked to same phase-id. On success, parse JSON for: `change_id`, `phase_id`, `phase_dir`, `plan_files`, `phase_link_path`, `gate_passed`.

    **Step 3: Invoke canonical GSD execute-phase** — Build the command: `gsd-execute-phase <phase_id>`. Append `--wave <N>` if `WAVE_FLAG` was supplied. Append `--gaps-only` if `GAPS_ONLY_FLAG` was set. If the runtime uses slash commands, invoke as `/gsd-execute-phase <phase_id> [--wave N] [--gaps-only]`. If direct command invocation is unavailable, instruct the user to run the canonical command exactly. Add explicit statement: "Do not copy or inline executor internals here. The canonical workflow is `.agents/gsd-core/workflows/execute-phase.md` (packaged source: `gsd-core/workflows/execute-phase.md`) and remains authoritative for wave grouping, subagent spawning, plan execution, summary writing, and verification."

    **Step 4: Report execution result** — After GSD execute-phase completes, report: the NDD change id, phase id, and the traceability path from change-id → phase-link.md → phase-dir → SUMMARY.md/VERIFICATION.md. Note that GSD writes SUMMARY.md and VERIFICATION.md in the phase directory. NDD does not update STATUS.json or phase-link.md post-execution (per D-07).

    Replace `<success_criteria>` with:
    - Missing or invalid phase-link.md stops before execution.
    - A concrete GSD phase id with existing plan files is required before delegation.
    - `gsd-execute-phase <phase-id>` with `--wave` and `--gaps-only` passthrough is the only execution path.
    - GSD executor writes SUMMARY.md and VERIFICATION.md in the phase directory.
    - No parallel NDD executor core is introduced.
    - NDD does not update STATUS.json or phase-link.md after execution.
  </action>
  <acceptance_criteria>
    - `commands/ndd/execute-phase.md` contains frontmatter with `name: ndd:execute-phase`.
    - The `<process>` section has exactly 4 numbered steps.
    - Step 2 calls `gsd-tools ndd execute <INPUT_ID>` and parses JSON output.
    - Step 3 calls `gsd-execute-phase <phase_id>` with optional `--wave N` and `--gaps-only` passthrough.
    - The file contains the literal string `Do not copy or inline executor internals`.
    - The file references `.agents/gsd-core/workflows/execute-phase.md`.
    - The file does NOT contain `STATUS.json` update logic or `writeStatus` calls.
    - The `<success_criteria>` section includes at least 5 concrete criteria.
    - `node scripts/check-workflow-size.cjs commands/ndd/execute-phase.md` passes (if available).
  </acceptance_criteria>
  <verify>
    <automated>rg -n "gsd-execute-phase|gsd-tools ndd execute|Do not copy or inline executor internals" commands/ndd/execute-phase.md</automated>
    <automated>rg -n "execute-phase.md" commands/ndd/execute-phase.md</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/user input to router | Untrusted change-id or phase-id enters dual-path resolution and filesystem reads. |
| phase-link.md content to structured output | User-writable Markdown parsed with regex becomes router JSON output. |
| NDD workflow to GSD executor | NDD-resolved phase-id becomes GSD execute-phase input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | `execute` subcommand input | high | mitigate | Reuse existing `validateChangeId` and `locateCurrentPhase` for input validation; never concatenate unvalidated input into file paths. |
| T-06-02 | Tampering | phase-link.md content | medium | mitigate | Parse with strict regex anchored to line start; reject null parse results before using values in filesystem operations. |
| T-06-03 | Elevation of Privilege | Workflow handoff | medium | mitigate | Workflow passes phase-id to canonical GSD execute-phase; no arbitrary shell command construction. |
| T-06-04 | Denial of Service | Reverse lookup scan | low | accept | O(n) scan of changes/ directories acceptable for v1 change counts. |
</threat_model>

<verification>
Overall checks: `npx tsc --project tsconfig.build.json`, `npx eslint src/ndd-command-router.cts`, and grep verification that the workflow references canonical GSD execute-phase plus non-duplication statement.
</verification>

<success_criteria>
Plan 06-01 is complete when `gsd-tools ndd execute <id>` resolves change-id or phase-id to execution-ready JSON with gate enforcement, and `commands/ndd/execute-phase.md` is a complete workflow adapter that delegates to canonical GSD execute-phase without duplicating executor internals.
</success_criteria>

<output>
Create `.planning/phases/06-execution-bridge/06-01-SUMMARY.md` when done.
</output>
