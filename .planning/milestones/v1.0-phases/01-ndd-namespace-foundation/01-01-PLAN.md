---
phase: 01-ndd-namespace-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/runtime-artifact-layout.cts
  - src/install-profiles.cts
  - gsd-core/bin/lib/runtime-artifact-layout.cjs
  - gsd-core/bin/lib/install-profiles.cjs
autonomous: true
requirements: [NDD-01, MNT-01, MNT-02]
user_setup: []
must_haves:
  truths:
    - "D-01/D-04: Runtime artifact layout can represent a parallel top-level non-GSD command namespace without changing existing GSD namespace behavior."
    - "D-05: The namespace abstraction leaves a path to future capability/plugin registration but does not implement a full plugin/capability system in Phase 1."
    - "D-07/D-11: Phase 1 namespace support is limited to local install layouts; global NDD installation remains out of scope."
    - "D-12: Existing gsd-* local Codex and Antigravity artifact layout behavior remains unchanged."
  artifacts:
    - "src/runtime-artifact-layout.cts contains a reusable command namespace/source-dir abstraction instead of assuming only commands/gsd for skills layouts."
    - "src/install-profiles.cts can stage runtime skills from a caller-provided command namespace source directory and prefix."
  key_links:
    - "Capability artifact layout descriptors can select commands/ndd as a source namespace while reusing the existing command-to-skill converter pipeline."
---

<objective>
Add the installer/runtime artifact abstraction needed for NDD to exist as a parallel command namespace.

Purpose: NDD must be plug-and-play over GSD Core, so namespace support belongs in reusable staging/layout mechanics rather than in copied workflow cores.
Output: Source-level support for descriptor-driven command namespaces, with current `gsd-*` runtime layouts preserved.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ndd-namespace-foundation/01-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@src/runtime-artifact-layout.cts
@src/install-profiles.cts
@capabilities/codex/capability.json
@capabilities/antigravity/capability.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add descriptor fields for command namespace source selection</name>
  <files>src/runtime-artifact-layout.cts</files>
  <read_first>src/runtime-artifact-layout.cts, capabilities/codex/capability.json, capabilities/antigravity/capability.json, .planning/phases/01-ndd-namespace-foundation/01-CONTEXT.md</read_first>
  <action>Extend the runtime artifact kind descriptor shape so a `skills` or `commands` kind can specify a command namespace source, using a concrete field such as `sourceNamespace: "gsd" | "ndd"` or `sourceSubpath: "commands/gsd" | "commands/ndd"`. Keep the default behavior equivalent to `commands/gsd` when the new field is omitted. Update source discovery by replacing the GSD-only `findInstallSourceRoot(configDir)` assumption with a helper that resolves the requested command namespace from the `.gsd-source` marker or by walking upward from `__dirname`. The helper must resolve `commands/gsd` for current descriptors and `commands/ndd` for future NDD descriptors.</action>
  <verify>npm run build:lib</verify>
  <acceptance_criteria>
    - `src/runtime-artifact-layout.cts` still defaults omitted namespace/source fields to `commands/gsd`.
    - `src/runtime-artifact-layout.cts` contains a helper that can resolve `commands/ndd` without hard-coding NDD into converter functions.
    - `npm run build:lib` exits 0 and regenerates `gsd-core/bin/lib/runtime-artifact-layout.cjs`.
  </acceptance_criteria>
  <done>Runtime layout descriptors can address non-GSD command sources while old descriptors remain byte-behavior compatible.</done>
</task>

<task type="auto">
  <name>Task 2: Thread namespace source through command-to-skill staging</name>
  <files>src/install-profiles.cts, src/runtime-artifact-layout.cts</files>
  <read_first>src/install-profiles.cts, src/runtime-artifact-layout.cts, src/runtime-artifact-conversion.cts</read_first>
  <action>Keep `stageSkillsForRuntimeAsSkills(srcCommandsDir, resolvedProfile, converter, prefix, nested)` as the shared staging primitive, but make all runtime layout callers pass the resolved namespace source directory explicitly. If profile filtering uses `loadSkillsManifest`, ensure manifest loading continues to use `commands/gsd` for normal GSD profiles and does not accidentally require NDD stems to be present in GSD profiles. Do not introduce a separate NDD profile system in this phase.</action>
  <verify>npm run build:lib</verify>
  <acceptance_criteria>
    - Existing `stageSkillsForRuntimeAsSkills` behavior for `commands/gsd` and prefix `gsd-` is unchanged.
    - NDD staging can use the same staging primitive with prefix `ndd-`.
    - No converter in `src/runtime-artifact-conversion.cts` contains NDD-specific branching.
  </acceptance_criteria>
  <done>Namespace selection is handled before conversion, so NDD reuses the core conversion pipeline.</done>
</task>

<task type="auto">
  <name>Task 3: Build generated CommonJS outputs from TypeScript source</name>
  <files>gsd-core/bin/lib/runtime-artifact-layout.cjs, gsd-core/bin/lib/install-profiles.cjs</files>
  <read_first>package.json, tsconfig.json, src/runtime-artifact-layout.cts, src/install-profiles.cts</read_first>
  <action>Run the repository build command that compiles `src/**/*.cts` into `gsd-core/bin/lib/*.cjs`. Keep generated CJS changes limited to the TypeScript source changes from this plan.</action>
  <verify>npm run build:lib</verify>
  <acceptance_criteria>
    - `gsd-core/bin/lib/runtime-artifact-layout.cjs` reflects the namespace source resolver changes.
    - `gsd-core/bin/lib/install-profiles.cjs` reflects any staging helper changes.
    - `git diff -- src/runtime-artifact-layout.cts gsd-core/bin/lib/runtime-artifact-layout.cjs` shows generated output corresponding to source edits, not unrelated formatting churn.
  </acceptance_criteria>
  <done>Runtime install code can execute the new namespace abstraction from generated artifacts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build:lib`
- [ ] `node --test tests/runtime-artifact-layout.test.cjs`
- [ ] Inspect Codex and Antigravity layout tests to confirm existing `gsd-` assertions still pass.
</verification>

<success_criteria>
- The runtime artifact layer can resolve both `commands/gsd` and a future `commands/ndd` source directory.
- Existing GSD runtime layout descriptor behavior is unchanged when no namespace/source override is declared.
- No full NDD plugin system or global install support is introduced in this plan.
</success_criteria>

## Artifacts this phase produces

- Descriptor field: `sourceNamespace` or equivalent command-source selector on artifact layout kind entries.
- Helper: command namespace source resolver in `src/runtime-artifact-layout.cts`.
- Generated file updates: `gsd-core/bin/lib/runtime-artifact-layout.cjs`, `gsd-core/bin/lib/install-profiles.cjs`.

<output>
After completion, create `.planning/phases/01-ndd-namespace-foundation/01-01-SUMMARY.md`.
</output>
