---
phase: 01-ndd-namespace-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - commands/ndd/change.md
  - commands/ndd/impact.md
  - commands/ndd/discuss-phase.md
  - commands/ndd/plan-phase.md
  - commands/ndd/execute-phase.md
  - commands/ndd/verify-work.md
  - commands/ndd/ship.md
  - capabilities/codex/capability.json
  - capabilities/antigravity/capability.json
  - scripts/gen-plugin-skills.cjs
  - skills/ndd-change/SKILL.md
  - skills/ndd-impact/SKILL.md
  - skills/ndd-discuss-phase/SKILL.md
  - skills/ndd-plan-phase/SKILL.md
  - skills/ndd-execute-phase/SKILL.md
  - skills/ndd-verify-work/SKILL.md
  - skills/ndd-ship/SKILL.md
autonomous: true
requirements: [NDD-01, NDD-02, MNT-01]
user_setup: []
must_haves:
  truths:
    - "D-02/D-03: `commands/ndd/*.md` is the canonical NDD command source and committed `skills/ndd-*` output is generated from it where possible."
    - "D-06: The direct NDD command names are `ndd-change`, `ndd-impact`, `ndd-discuss-phase`, `ndd-plan-phase`, `ndd-execute-phase`, `ndd-verify-work`, and `ndd-ship`."
    - "D-14/D-17/D-18: NDD wrappers reference and dispatch into GSD mechanics; they do not copy full GSD core workflows."
    - "D-15: NDD wrappers may call existing `gsd-tools.cjs` helpers instead of introducing a parallel helper runtime."
    - "D-16: NDD wrappers may generate bridge artifacts consumable by existing GSD planner/executor/verifier mechanics."
  artifacts:
    - "commands/ndd/change.md"
    - "commands/ndd/impact.md"
    - "commands/ndd/discuss-phase.md"
    - "commands/ndd/plan-phase.md"
    - "commands/ndd/execute-phase.md"
    - "commands/ndd/verify-work.md"
    - "commands/ndd/ship.md"
    - "skills/ndd-change/SKILL.md"
    - "skills/ndd-impact/SKILL.md"
    - "skills/ndd-discuss-phase/SKILL.md"
    - "skills/ndd-plan-phase/SKILL.md"
    - "skills/ndd-execute-phase/SKILL.md"
    - "skills/ndd-verify-work/SKILL.md"
    - "skills/ndd-ship/SKILL.md"
  key_links:
    - "Codex and Antigravity local artifact layouts declare both GSD skills and NDD skills using the same converter pipeline with different command sources and prefixes."
---

<objective>
Create the NDD command and generated skill surface for local Codex and Antigravity installs.

Purpose: Users should see NDD as a distinct branded workflow namespace while implementation stays as small wrappers over GSD Core.
Output: Canonical `commands/ndd/*.md`, generated `skills/ndd-*`, and runtime descriptors that stage NDD locally.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-ndd-namespace-foundation/01-CONTEXT.md
@commands/gsd/plan-phase.md
@commands/gsd/execute-phase.md
@commands/gsd/verify-work.md
@commands/gsd/ship.md
@scripts/gen-plugin-skills.cjs
@capabilities/codex/capability.json
@capabilities/antigravity/capability.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical NDD command wrappers</name>
  <files>commands/ndd/change.md, commands/ndd/impact.md, commands/ndd/discuss-phase.md, commands/ndd/plan-phase.md, commands/ndd/execute-phase.md, commands/ndd/verify-work.md, commands/ndd/ship.md</files>
  <read_first>commands/gsd/discuss-phase.md, commands/gsd/plan-phase.md, commands/gsd/execute-phase.md, commands/gsd/verify-work.md, commands/gsd/ship.md, .planning/phases/01-ndd-namespace-foundation/01-CONTEXT.md</read_first>
  <action>Create `commands/ndd/` with seven Markdown command files. Each file must use the existing command frontmatter style used by `commands/gsd/*.md`. `change.md` and `impact.md` may be V1 placeholder wrappers that explain the local NDD artifact intent and defer full behavior to later phases, but they must not claim completed Phase 2/3 behavior. `discuss-phase.md`, `plan-phase.md`, `execute-phase.md`, `verify-work.md`, and `ship.md` must explicitly state that they load NDD artifacts and then reuse the matching GSD phase mechanics. Do not paste full contents from `commands/gsd/plan-phase.md`, `commands/gsd/execute-phase.md`, `commands/gsd/verify-work.md`, or `commands/gsd/ship.md` into NDD files.</action>
  <verify>test -f commands/ndd/change.md && test -f commands/ndd/ship.md</verify>
  <acceptance_criteria>
    - `commands/ndd/` contains exactly the seven V1 command files named in D-06.
    - Each `commands/ndd/*.md` file contains the string `NDD` and references at least one GSD workflow, helper, or command where reuse is intended.
    - `commands/ndd/plan-phase.md` does not contain a full copy of `.agents/gsd-core/workflows/plan-phase.md`.
    - `commands/ndd/execute-phase.md` does not contain a full copy of `.agents/gsd-core/workflows/execute-phase.md`.
  </acceptance_criteria>
  <done>NDD has a canonical command source surface that is thin by design.</done>
</task>

<task type="auto">
  <name>Task 2: Declare local NDD skills for Codex and Antigravity descriptors</name>
  <files>capabilities/codex/capability.json, capabilities/antigravity/capability.json</files>
  <read_first>capabilities/codex/capability.json, capabilities/antigravity/capability.json, src/runtime-artifact-layout.cts</read_first>
  <action>For the `local` artifact layout of Codex and Antigravity, add a second `skills` kind entry for NDD with `destSubpath: "skills"`, `prefix: "ndd-"`, `nesting: "flat"`, `recursive: false`, the same runtime-specific converter as the existing GSD entry, and the namespace/source selector from Plan 01 pointing at `commands/ndd`. Leave existing `global` layout entries unchanged for NDD Phase 1. Keep the existing GSD `local` entries intact.</action>
  <verify>node --test tests/runtime-artifact-layout.test.cjs</verify>
  <acceptance_criteria>
    - `capabilities/codex/capability.json` has both a local `gsd-` skills entry and a local `ndd-` skills entry.
    - `capabilities/antigravity/capability.json` has both a local `gsd-` skills entry and a local `ndd-` skills entry.
    - Neither capability file adds an NDD global artifact layout entry.
    - `node --test tests/runtime-artifact-layout.test.cjs` exits 0 after tests are updated in Plan 03.
  </acceptance_criteria>
  <done>Local runtime artifact layout can surface NDD skills next to GSD skills.</done>
</task>

<task type="auto">
  <name>Task 3: Generalize generated plugin skills for multiple command namespaces</name>
  <files>scripts/gen-plugin-skills.cjs, skills/ndd-change/SKILL.md, skills/ndd-impact/SKILL.md, skills/ndd-discuss-phase/SKILL.md, skills/ndd-plan-phase/SKILL.md, skills/ndd-execute-phase/SKILL.md, skills/ndd-verify-work/SKILL.md, skills/ndd-ship/SKILL.md</files>
  <read_first>scripts/gen-plugin-skills.cjs, src/runtime-artifact-conversion.cts, commands/ndd/change.md</read_first>
  <action>Update `scripts/gen-plugin-skills.cjs` so it generates committed skill directories for both `commands/gsd` with prefix `gsd-` and `commands/ndd` with prefix `ndd-`. Preserve stale-check behavior for existing `gsd-*` skills. Add stale-check behavior for `ndd-*` skills. Run the generation script with `--write` after `npm run build:lib` so `skills/ndd-*/SKILL.md` exists and is generated from `commands/ndd/*.md`.</action>
  <verify>npm run build:lib && node scripts/gen-plugin-skills.cjs --check</verify>
  <acceptance_criteria>
    - `node scripts/gen-plugin-skills.cjs --check` exits 0 after generation.
    - `skills/ndd-change/SKILL.md` exists and includes the generated skill name `ndd-change`.
    - Existing `skills/gsd-plan-phase/SKILL.md` remains generated from `commands/gsd/plan-phase.md`.
    - The script does not remove non-`gsd-*` and non-`ndd-*` directories under `skills/`.
  </acceptance_criteria>
  <done>Committed skill artifacts include the NDD namespace without hand-maintained drift.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build:lib`
- [ ] `node scripts/gen-plugin-skills.cjs --check`
- [ ] `node --test tests/runtime-artifact-layout.test.cjs`
</verification>

<success_criteria>
- NDD direct command names are present as canonical Markdown command files.
- Local Codex and Antigravity descriptors can stage `ndd-*` skills from `commands/ndd`.
- NDD wrappers are small adapters over GSD mechanics and do not fork GSD workflow internals.
</success_criteria>

## Artifacts this phase produces

- New directory: `commands/ndd/`
- New command files: `change.md`, `impact.md`, `discuss-phase.md`, `plan-phase.md`, `execute-phase.md`, `verify-work.md`, `ship.md`
- New generated skill directories: `skills/ndd-change`, `skills/ndd-impact`, `skills/ndd-discuss-phase`, `skills/ndd-plan-phase`, `skills/ndd-execute-phase`, `skills/ndd-verify-work`, `skills/ndd-ship`
- Capability descriptor entries: Codex local `ndd-` skills entry; Antigravity local `ndd-` skills entry

<output>
After completion, create `.planning/phases/01-ndd-namespace-foundation/01-02-SUMMARY.md`.
</output>
