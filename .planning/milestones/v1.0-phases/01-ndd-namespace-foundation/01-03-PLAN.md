---
phase: 01-ndd-namespace-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - tests/runtime-artifact-layout.test.cjs
  - tests/runtime-artifact-layout-install-profiles.test.cjs
  - tests/install-runtime-artifacts.test.cjs
  - tests/bug-3659-applysurface-prune-skill-dirs.test.cjs
  - tests/commands.test.cjs
  - tests/agent-skills.test.cjs
autonomous: true
requirements: [NDD-01, NDD-02, MNT-01, MNT-02]
user_setup: []
must_haves:
  truths:
    - "D-08/D-09/D-13: Local Codex and Antigravity installs produce `.codex/skills/ndd-*` and `.agents/skills/ndd-*` from `commands/ndd/*.md`."
    - "D-10/D-11: Tests prove local artifact output only and do not assert global NDD support."
    - "D-12: Existing GSD command installation and generated `gsd-*` skills still pass their current tests."
    - "D-13: Namespace cleanup/pruning does not remove unrelated user-authored skills."
  artifacts:
    - "Updated runtime artifact layout tests covering `ndd-` local descriptor entries."
    - "Install/runtime tests covering local Codex and local Antigravity NDD skill output."
    - "Generation/check tests covering `skills/ndd-*` staleness."
  key_links:
    - "Tests connect `commands/ndd/*.md` to installed `.codex/skills/ndd-*/SKILL.md` and `.agents/skills/ndd-*/SKILL.md` outputs."
---

<objective>
Add regression coverage that proves NDD namespace behavior works locally and does not regress GSD.

Purpose: Installer/runtime artifact code is high-risk; the NDD namespace must be locked down by tests before later workflow phases depend on it.
Output: Focused tests for descriptor shape, staging, generated skills, local installs, and cleanup boundaries.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-ndd-namespace-foundation/01-CONTEXT.md
@tests/runtime-artifact-layout.test.cjs
@tests/runtime-artifact-layout-install-profiles.test.cjs
@tests/install-runtime-artifacts.test.cjs
@tests/bug-3659-applysurface-prune-skill-dirs.test.cjs
@tests/commands.test.cjs
@tests/agent-skills.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cover descriptor shape for dual local namespaces</name>
  <files>tests/runtime-artifact-layout.test.cjs</files>
  <read_first>tests/runtime-artifact-layout.test.cjs, capabilities/codex/capability.json, capabilities/antigravity/capability.json, src/runtime-artifact-layout.cts</read_first>
  <action>Update Codex and Antigravity local layout assertions so they expect two local `skills` kind entries: one with prefix `gsd-` sourced from `commands/gsd`, and one with prefix `ndd-` sourced from `commands/ndd`. Keep global layout assertions for Codex and Antigravity expecting only the existing GSD entry. Add assertions for the concrete source selector field introduced in Plan 01.</action>
  <verify>node --test tests/runtime-artifact-layout.test.cjs</verify>
  <acceptance_criteria>
    - Codex local layout test asserts both `gsd-` and `ndd-` skill kinds.
    - Antigravity local layout test asserts both `gsd-` and `ndd-` skill kinds.
    - Codex global and Antigravity global tests do not expect NDD entries.
    - `node --test tests/runtime-artifact-layout.test.cjs` exits 0.
  </acceptance_criteria>
  <done>Descriptor tests prove local-only NDD namespace declaration.</done>
</task>

<task type="auto">
  <name>Task 2: Cover staging and generated skill conversion for NDD</name>
  <files>tests/runtime-artifact-layout-install-profiles.test.cjs, tests/commands.test.cjs, tests/agent-skills.test.cjs</files>
  <read_first>tests/runtime-artifact-layout-install-profiles.test.cjs, tests/commands.test.cjs, tests/agent-skills.test.cjs, scripts/gen-plugin-skills.cjs, commands/ndd/change.md</read_first>
  <action>Add assertions that NDD command files are discoverable as command artifacts and that generated `skills/ndd-*/SKILL.md` files are in sync with `commands/ndd/*.md`. Ensure existing GSD command and skill checks continue to run unchanged. If the existing tests assume every skill starts with `gsd-`, update that assumption to allow the committed `ndd-*` generated namespace while still rejecting unexpected stale generated directories.</action>
  <verify>node --test tests/runtime-artifact-layout-install-profiles.test.cjs tests/commands.test.cjs tests/agent-skills.test.cjs</verify>
  <acceptance_criteria>
    - Test output proves `commands/ndd/change.md` maps to `skills/ndd-change/SKILL.md`.
    - `node scripts/gen-plugin-skills.cjs --check` exits 0 during or before the test run.
    - Existing `commands/gsd/*.md` and `skills/gsd-*` checks still pass.
  </acceptance_criteria>
  <done>Generated skill drift checks include NDD without weakening GSD checks.</done>
</task>

<task type="auto">
  <name>Task 3: Cover local Codex and Antigravity install output plus cleanup boundary</name>
  <files>tests/install-runtime-artifacts.test.cjs, tests/bug-3659-applysurface-prune-skill-dirs.test.cjs</files>
  <read_first>tests/install-runtime-artifacts.test.cjs, tests/bug-3659-applysurface-prune-skill-dirs.test.cjs, src/install-engine.cts, src/runtime-artifact-layout.cts</read_first>
  <action>Add or extend install fixture tests so a local Codex install writes `.codex/skills/ndd-change/SKILL.md` and a local Antigravity install writes `.agents/skills/ndd-change/SKILL.md`. Assert that `.codex/skills/gsd-plan-phase/SKILL.md` and `.agents/skills/gsd-plan-phase/SKILL.md` still exist in the same local install. Extend the pruning regression to prove a user-authored skill directory that does not belong to GSD or NDD is preserved, and that adding NDD does not broaden stale cleanup beyond the managed prefixes.</action>
  <verify>node --test tests/install-runtime-artifacts.test.cjs tests/bug-3659-applysurface-prune-skill-dirs.test.cjs</verify>
  <acceptance_criteria>
    - Local Codex fixture contains `.codex/skills/ndd-change/SKILL.md`.
    - Local Antigravity fixture contains `.agents/skills/ndd-change/SKILL.md`.
    - The same fixture still contains representative `gsd-*` skills.
    - A fixture user skill such as `.codex/skills/user-owned/SKILL.md` is not removed by install or surface cleanup.
    - No test asserts or requires global NDD install output.
  </acceptance_criteria>
  <done>Install tests prove the Phase 1 boundary and preservation guarantees.</done>
</task>

<task type="auto">
  <name>Task 4: Run focused regression suite for namespace foundation</name>
  <files>tests/runtime-artifact-layout.test.cjs, tests/runtime-artifact-layout-install-profiles.test.cjs, tests/install-runtime-artifacts.test.cjs, tests/commands.test.cjs, tests/agent-skills.test.cjs, tests/bug-3659-applysurface-prune-skill-dirs.test.cjs</files>
  <read_first>package.json, tests/helpers.cjs</read_first>
  <action>Run the focused Node test files that cover runtime artifact layout, install profiles, local runtime install, command validation, skill generation, and pruning. If one of these tests fails because generated CJS is stale, run `npm run build:lib` and rerun the focused tests. Do not broaden the scope into unrelated runtime support beyond Codex and Antigravity unless a shared regression requires it.</action>
  <verify>node --test tests/runtime-artifact-layout.test.cjs tests/runtime-artifact-layout-install-profiles.test.cjs tests/install-runtime-artifacts.test.cjs tests/commands.test.cjs tests/agent-skills.test.cjs tests/bug-3659-applysurface-prune-skill-dirs.test.cjs</verify>
  <acceptance_criteria>
    - Focused Node test command exits 0.
    - Any test failure due to stale generated files is resolved by rebuilding generated CJS, not by weakening assertions.
    - Test names or assertions explicitly mention local Codex NDD and local Antigravity NDD behavior.
  </acceptance_criteria>
  <done>Phase 1 has executable regression coverage for the namespace foundation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build:lib`
- [ ] `node scripts/gen-plugin-skills.cjs --check`
- [ ] `node --test tests/runtime-artifact-layout.test.cjs tests/runtime-artifact-layout-install-profiles.test.cjs tests/install-runtime-artifacts.test.cjs tests/commands.test.cjs tests/agent-skills.test.cjs tests/bug-3659-applysurface-prune-skill-dirs.test.cjs`
</verification>

<success_criteria>
- Local Codex and Antigravity tests prove NDD skills are emitted under `.codex/skills/ndd-*` and `.agents/skills/ndd-*`.
- Existing GSD install and generated skill tests still pass.
- Tests lock Phase 1 to local NDD support and leave global NDD support for a later phase.
</success_criteria>

## Artifacts this phase produces

- Updated runtime artifact descriptor tests for dual local namespace entries.
- Updated install/profile tests covering NDD local skill generation.
- Updated generated skill drift tests for `ndd-*`.
- Updated pruning regression coverage for managed prefix boundaries.

<output>
After completion, create `.planning/phases/01-ndd-namespace-foundation/01-03-SUMMARY.md`.
</output>
